# 07 — Английские изображения кейса

Зависимость: 06 готов. Работай по `STATE.md`. Результат: 11 проверенных WebP-файлов в `public/media/case-learn/`.

## Мини-задачи

1. Прочитай `scripts/shoot-learn-frames.mjs`, EN-карту медиа в `ds/screens/case-learn.md` и подписи в `src/copy/cases/learn.ts`. Используй preview из этапа 06; если он остановлен, запусти снова.
2. Добавь в существующий скрипт простой необязательный выбор локали и отдельных кадров, чтобы снять только EN и переснимать лишь исправленные кадры. Сохрани прежний режим обеих локалей по умолчанию. Не делай второй независимый генератор.
3. Сними реальные страницы английского демо: home, trajectory/proekt, material/onvif-not-found, my, practice с видимым разбором неверного ответа, assessment/result и landing. Нужны семь основных файлов и три обложки `cover/home.webp`, `cover/trajectory.webp`, `cover/material.webp` размером 2000×1250.
4. Добавь одиннадцатый файл `before-catalog.webp` из `D:/Claude-projects/learn/images/default-pages/3.png` конвертацией в WebP. Архивный русский UI остаётся оригинальным; подпись EN должна это объяснять.
5. Для my/practice/result используй существующие state-marina-one/ready/final fixtures из `learn/audit/product-polish/evidence/07/`. Проверяй реальное выбранное состояние, а не только успешный screenshot. Practice должен показывать именно ошибку, обещанную подписью.
6. Проверь наличие, размеры, читаемость и соответствие подписи всех 11 изображений. Просмотри компактный обзор и отдельно проблемные/ключевые кадры. Демоназвание компании допускается локализовать в данных съёмки последовательно; произвольные пользовательские данные runtime не изменять.

Новые экраны снимаются из живого кода. Не генерировать UI картинками и не дорисовывать перевод поверх русского экрана. `PLAYWRIGHT_REPO` по умолчанию указывает на `../b2b-dssl` только как источник зависимости; тот проект не менять.

## Приёмка и остановка

Все 11 EN-файлов готовы, реальные состояния совпадают с кейсом. Запиши команду фильтрованной съёмки и итог в STATE.md. Если исправлялся исходник продукта, сначала повтори затронутую проверку, обнови экспорт/preview и лишь затем снимай соответствующий кадр. Финальная сборка кейса — после RU-съёмки, в 09.
