# Приведение портфолио к найму — находки

Дата: 2026-08-28.

**Источник.** Разбор сайта с двух позиций — рекрутер, закрывающий вакансию Product Designer в B2B SaaS, и лид, который берёт человека в команду. Каждая находка сверена по исходникам репозитория, а не по впечатлению от страницы: `src/copy/`, `ds/screens/`, `ds/patterns.md`, `cv/resume-data.json`, `linkedin/execution-plan.md`.

**Что этот список не покрывает.** Визуальный язык, движение, производительность и доступность — они закрываются своими проходами (Чаты 6–9 плейбука) и здесь не дублируются. Здесь только то, что меняет решение «звать или не звать».

---

## Правило уровней

Сохраняется без изменений — `PLAYBOOK-site-portfolio.md` §Чат 7:

> `L2` чинится в источнике (`ds/`) и пропагируется, `L1` правится на месте, `L3` эскалируется в `ia/open-questions.md` и молча не правится. Сначала все `L2`, потом `L1`.

Два уточнения для этого списка:

1. **Носитель источника для текстов — не `ds/`, а карта экрана.** Правка формулировки, которая стоит больше чем в одном месте, идёт через `ds/screens/<name>.md` и `ia/wireframes/`, а `src/copy/` — зеркало. Русская локаль — часть зеркала: у каждой строки в `src/copy/` есть пара в `src/copy/ru/`, и правка без пары считается незакрытой.
2. **Структурная находка после сборки автоматически `L3`** — Structure Pass по кейсам не проводился (`PLAYBOOK` §Чат 8). Решение «пересобирать или жить с этим» принимает владелец.

Структурные `L3` продублированы в `ia/open-questions.md` строками 11–13. Остальные `L3` — факты о владельце и документы вне контура ИА (`cv/`), в открытые вопросы ИА они по правилу самого файла не идут: там только то, что влияет на структуру.

---

## L2 — правится в источнике, расходится на маршруты

| # | Находка | Почему `L2` | Где правится | Готово |
|---|---|---|---|---|
| ~~L2-1~~ | ~~**Тип доказательства не назван.**~~ — **закрыто 28.08.2026** | Пара `Evidence` / `Доказательство` встала шестой в мете: `Paid client · user-tested · accepted` / `Commercial redesign · shipped in full` / `Concept · domain input from one vet` / `Independent concept · no human validation`. Строка на обложке и в шапке кейса совпадает дословно — обложка кейса в двух местах остаётся одним объектом | **Шестнадцать носителей, не двенадцать**: 4 карточки главной и 4 шапки кейсов, по каждой — русская пара. Канонический разбор решения — `ds/screens/home.md` §FeaturedCase, карты кейсов ссылаются на него | ⚠ **частично проверено.** Сборка проходит, 15 страниц. Переполнения на узкой ширине быть не может: `MetaList.astro` держит `layout=inline` на `flex-wrap: wrap` с `column-gap: space-6` — шестая пара переносится, а не выдавливает строку. Не снято скриншотом только одно: во сколько строк мета встаёт на 360 px и не спорит ли выросшая шапка со строкой исхода. Playwright в `node_modules` нет, прогон идёт отдельным шагом |
| ~~L2-2~~ | ~~**Инвентарь сборки стоит в позиции результата.**~~ — **закрыто 28.08.2026, формулировка находки была неверной** | Проверка по исходникам: строки `Built` нет у DSSL и Vet Clinic OS вовсе — у них `CaseResult` идёт `Solved · Sacrificed · Why the price was right · What changed`. Опись стояла в двух кейсах из четырёх, Agent Ops и Pawly, и там же отсутствовала строка цены. То есть дефект не «слишком много чисел», а: **два кейса, потерявшие формулировку цены, — ровно те, что приобрели счётчики** | Инвентарь ДС оказался вторым носителем: те же 42 компонента, 331 вариант, 88 + 70 токенов уже стоят в `CaseSystem`, у Pawly — то же с 33 компонентами. Снято удалением, не переносом: переносить некуда. Освободившаяся строка отдана `Sacrificed`. Четвёртой строкой у обоих остаётся `Verified`, а не `Why the price was right` — схема не выравнивается под DSSL до конца, и это против `L3-4`, а не за него | ✅ сборка 15 страниц. Бюджет слов пересчитан: `CaseResult` Agent Ops 304 → 301, итог страницы 2480 → 2477 против лимита `CASE-15`; Pawly 2 313 → 2 296. Снятые числа перечислены в картах |
| ~~L2-3~~ | ~~**Кейс заканчивается тупиком.**~~ — **закрыто 28.08.2026** | `CaseOutro` ведёт на следующий кейс, последний замыкается на первый. Это не новое решение, а возврат к тексту `CASE-16`: роль «дальше» отдавалась контакту **только в версии 1**, «с появлением второго кейса возвращается `Next case` без возврата на главную». Кейсов четыре | Надпись сквозная (`site.nextCase`), адрес считается по реестру `src/copy/cases/index.ts` — реестр остаётся единственным носителем порядка (`US-20`). Пары `cta` / `href` удалены из восьми файлов текстов как мёртвые данные. **Попутно снято расхождение, о котором репозиторий спорил сам с собой:** DSSL вёл на якорь `#contact` с проверкой по собранной странице, три соседних кейса — на `mailto:`, а карта Vet обосновывала `mailto:` тем, что якоря «на сайте не существует». Якорь существует; обе ошибки сняты вместе с CTA | ✅ сборка 15 страниц, `check:css` — ноль мёртвых правил. Проверены все восемь переходов в собранной разметке: цикл замыкается на обеих локалях, русские ведут на `/ru/work/…` |
| ~~L2-4~~ | ~~**Нет скан-слоя по длине кейса.**~~ — **снята 28.08.2026: находка сама себе противоречит** | Инструмент, который я предлагал, — указатель из заголовков-утверждений. Заголовки посчитаны: 97 слов у Agent Ops, 81 у Pawly, 75 у Vet, 71 у DSSL. Запас до лимита по картам: 23 слова у Agent Ops, 73 у Vet, 204 у Pawly, 400 у DSSL. То есть указатель ломает `CASE-15` на двух кейсах из четырёх (Agent Ops → ~2 574, Vet → ~2 502) | И дело не в арифметике. `CASE-15` назван в требовании «бесплатным, но **жёстким**» с обоснованием «кейс длиннее ~10 минут чтения даёт отказ» — то есть лимит существует ровно против того отказа, который находка и лечит. **Строить скан-слой, добавляя нагрузку чтения, значит лечить длину длиной** | Остаток, который может захотеть владелец: бессловесная рельса-указатель — деления по числу секций, текущее закрашено, доступное имя берётся из заголовка. Ноль слов на странице; даёт длину, позицию и переход, но не даёт довод — довод по-прежнему собирается прокруткой. Цена: новый компонент, движение, поведение на мобильном. Моя оценка: не стоит того, решение владельца |
| L2-5 | **Кадры показывают экран целиком там, где нужен тезис.** — **два кадра из четырёх закрыты 28.08.2026, два вынесены** | **Замер, который всё определил:** `DecisionBlock` кладёт артефакт в колонку `0.6fr` — это ~453 px при контейнере 1232, а не ширина кейса. Полный кадр 2000 px шёл там в масштабе **0,23**: подпись обещала «$4,200» и «one expired article», прочитать их было нельзя. Кропы дают 0,53 и 0,40 | `MediaFrame` получил `zoomSrc`: на странице кроп с доводом, в зуме — тот же экран целиком. Кропы делает `scripts/crop-artifacts.mjs` на `sharp`, исходники не перезаписываются. Подпись кластера сокращена вместе с кадром — таблица участников в кроп не влезла, и обещать её больше нельзя. Альтернатива «отдать артефакту всю ширину» (масштаб 0,62) отклонена: +2 800 px к странице на 11 000 | ✅ сборка 15 страниц, все четыре ссылки на кропы разрешаются. **Поймано до отчёта:** `withRussianMedia` переписывает путь в `/media/case-agent-ops-ru`, и первая версия оставила русскую локаль с битыми картинками — русские кропы сделаны и проверены глазами отдельно. **Осталось:** `trace-gap-state` (сопоставление четырёх состояний в ряд — нужна пересъёмка в две колонки, кроп не поможет) и `autonomy` (довод в полноте лестницы). Плюс те же кропы в DSSL, Vet и Pawly |
| ~~L2-6~~ | ~~**Ссылка на живой прототип поднимается в шапку строкой меты.**~~ — **закрыто 28.08.2026** | Седьмая пара `Prototype` / `Прототип`, значение — ссылка: `Live, on synthetic data` у DSSL, `Live, on invented data` у трёх остальных. Прежний запрет на шапку снят разбором, а не отменой: довод «спорил бы с CTA за одну цель» относился к обложке на главной, где CTA есть, — в шапке кейса его нет; довод «обещал бы продукт» остался в силе и решён формулировкой, слово о данных стоит внутри самой ссылки. Ссылка в `CaseProcess` оставлена: в шапке она отвечает на «есть ли что открыть», там — на «то самое, через что прошёл агентский прогон». Адрес вынесен константой `PROTOTYPE`, чтобы носители не разошлись | Восемь носителей плюс правка компонента: `MetaList` получил необязательный `href`, значение уходит в `TextLink type=inline external`. Новым вариантом это не заводится — мёртвых веток не возникает. Записано в `ds/components.md`, `ds/patterns.md` §`CaseHeader`, `ds/screens/case-dssl.md`, `ds/screens/case-vet.md` | ✅ сборка 15 страниц, `check:css` — ноль мёртвых правил; в разметке проверены `target=_blank` + `rel=noopener noreferrer`, маркер ↗ и скрытая подпись внешней ссылки на обеих локалях |

---

## L1 — правится на месте

| # | Находка | Правка | Готово |
|---|---|---|---|
| ~~L1-1~~ | ~~**Секция Webflow обесценивает сама себя.**~~ — **закрыто 28.08.2026** | Стало `Selected web builds` / «Избранные веб-сборки» и подводка «вторая сторона практики: визуальный дизайн, доведённый до работающей адаптивной сборки». CMS в подводке не заявляется намеренно — коллекции есть только у Bloomlex, и общее утверждение было бы неверным на трёх сборках из четырёх | ☑ EN + RU |
| ~~L1-2~~ | ~~**Плашка DSSL называет только пересборку.**~~ — **закрыто 28.08.2026** | Метка стала `Shipped at DSSL — rebuilt here on synthetic data`, и первое предложение теперь про отгрузку, а не про пересборку. Ни один факт не снят: отгрузка и пересборка обе названы, поменялось, что стоит первым | ☑ EN + RU |
| ~~L1-3~~ | ~~**Vet не называет границу проверки.**~~ — **закрыто 28.08.2026** | `Validated: workflow and vocabulary, with one practising veterinarian. Not validated: whether either generalises across clinics, roles or regulation.` | ⚠ **порядок работы был нарушен, и это стоило прохода.** Карта Vet прямо требует начинать правку текста с пересчёта: страница шла в 2 499 слов при лимите 2 500. Я посчитал после, а не до. Плата взята ужатием двух мест без потери содержания — середина строки NDA и дубль «31 logical components» в `CaseSystem`, повторявший собственный заголовок блока. Страница вернулась на ~2 500. В карту записаны пересчёт, различие двух бухгалтерий и калибровка счётчика |

---

## L3 — не правится до решения владельца

| # | Вопрос | Варианты | Что меняется | Куда эскалировано |
|---|---|---|---|---|
| ~~L3-1~~ | ~~**Порядок фактов в строке права на работу.**~~ — **закрыто владельцем 28.08.2026, вариант (б)** | Стало: `Kazakhstan · remote now, open to relocation with visa sponsorship.` — 66 знаков, лимит в две строки на 360 px соблюдён. Ни один факт не снят, изменилась очерёдность. Полный абзац на `/about` не тронут: там очерёдность другая намеренно | `ia/authorization-copy.md` (источник), `src/copy/home.ts`, `src/copy/ru/home.ts` | закрыто |
| ~~L3-2~~ | ~~**Pawly: `Lead Product Designer` при `Sole designer`.**~~ — **закрыто владельцем 28.08.2026, вариант (а)** | Стало `Product Designer` / `Продуктовый дизайнер`. Проверено, что заявки на Lead не осталось нигде: ни в текстах кейса, ни в `cv/`, ни в `linkedin/change-set.md` | `src/copy/home.ts`, `src/copy/cases/pawly.ts`, `src/copy/ru/home.ts`, `src/copy/ru/cases/pawly.ts` | закрыто |
| ~~L3-3~~ | ~~**Живой прототип спрятан в середине кейса.**~~ — **закрыто владельцем 28.08.2026, вариант (б)** | Ссылка поднимается строкой меты в шапку. Правило `ds/patterns.md` о второй цели фокуса не нарушается | Переведено в `L2-6`: восемь носителей, меняется состав узла `CaseHeader` | `ia/open-questions.md` №11 — закрыт |
| ~~L3-4~~ | ~~**Четыре кейса идут по одному скелету.**~~ — **закрыто владельцем 28.08.2026, вариант (б)** | Пара «было → стало» встала в `CaseContext` DSSL. Показывается один экран и один вопрос — чем открывается рабочее место закупщика: старый отвечает баннером обучения и промо, новый тем, что блокирует работу | Из десяти кадров исходного портала публикуется один: остальные девять несут живые артикулы, склады и суммы. **Ретуши не потребовалось** — граница кропа проведена так, что карточка заказа с номером, кодом ДП и суммой осталась за кадром. Обрезать надёжнее, чем замазывать. Кадры идут во всю ширину колонки (масштаб 0,76–0,86), а не парой в ряд: тот же замер, что в `L2-5` | ⚠ **обещание плашки пришлось поправить.** Фраза «на всех экранах синтетические данные» с появлением архивного кадра стала бы ложной — плашка теперь разделяет пересобранные экраны и один архивный кадр. Иначе кейс нарушил бы `CASE-22` ровно тем действием, которым доказывает свою правдивость. Закрывает вопросы ИА №12 и №2 |
| ~~L3-5~~ | ~~**Нет плана измерения там, где нет метрик.**~~ — **закрыто владельцем 28.08.2026, вариант (в)** | Строка внутри существующего `CaseResult`, схема кейса не тронута. Agent Ops — ступени автономности за квартал; DSSL — доля строк, доходящих до заказа без менеджера, и споры о поставке, закрываемые указанием на исходную строку; Pawly — доля прогулок с доказательством, которое владелец действительно открыл | У Agent Ops критерий сведён к **одному** числу, совпадающему с тезисом кейса, а не к трём: страница шла в лимит. Оплачено снятием двух дублей — «данные вымышлены» стояло на странице трижды, «42 компонента» дублировало заголовок собственного блока | ✅ страница 2 490 → 2 495 слов, то есть план измерения обошёлся в пять слов |
| ~~L3-6~~ | ~~**CV: `Freelance Jan 2023 – Present` перекрывает `DSSL`**~~ — **закрыто владельцем 28.08.2026, вариант (б)** | Две сущности вместо одной: `Product Designer - client work, Jan 2023 - Present` держит только оплачиваемые заказы и прямо говорит, что последний контракт закрыт весной 2026; `Selected independent projects, 2026` держит Pawly и пересборку портала | Даты сверены с трудовой хронологией владельца, не выдуманы: проектная веб-работа январь — сентябрь 2023, DSSL с сентября 2023. Обе локали, оба PDF пересобраны | ✅ в `cv/fact-sheet.md` записано правило: портфолио-концепты больше никогда не стоят под строкой занятости — именно из-за этого «3+ years» читалось как включающее личные проекты |
| ~~L3-7~~ | ~~**CV: `English — B2 (Upper-Intermediate)`**~~ — **закрыто владельцем 28.08.2026, вариант (в)** | Стало `English - professional working proficiency` / «Английский — свободно для рабочих задач». Уровень **снят, а не поднят** | В `cv/fact-sheet.md` строка переведена из VERIFIED в OWNER DECISION с явным запретом: балла нет и добавлять нельзя, `C1` не должен появиться нигде | ✅ оба PDF пересобраны |

---

## Вне сайта — работы, а не находки

Правкой страниц не закрываются и в уровнях не участвуют. Стоят здесь, потому что без них список выше не даёт результата: сайт доводит до звонка, дальше работает другое.

| # | Работа | Почему |
|---|---|---|
| W-1 | Две презентации на 15 минут — Agent Ops и DSSL | На звонке ведёшь ты, а не читатель. 2 500 слов кейса вслух не проговариваются, а листать сайт на шаринге — это чтение, а не рассказ |
| W-2 | Письменные ответы на шесть вопросов, которые сайт задаёт сам | «Кто был вторым дизайнером», «что сказал бэкенд про строку происхождения», «почему 4% обязательств — верная оценка», «что с прототипом после приёмки», «Lead — кем руководил», «B2 — мы сейчас говорим по-английски, это B2?» |
| W-3 | Разговорный английский | Если это узкое место — оно решает больше, чем любая правка на сайте |
| W-4 | LinkedIn, фаза 7 — распространение и измерение | Профиль закрыт на 6 фаз из 7 (`linkedin/execution-plan.md`). Канал не включён, а при релокации со спонсорством трафик сам не приходит |
| W-5 | Один живой платный заказ, доведённый до прода с правом опубликовать хотя бы один измеренный показатель | Порог senior упирается в множественные отгруженные продукты с эффектом после запуска. Их сейчас один, без метрик по NDA и без baseline. Редактированием текста это не создаётся |

---

## Намеренно не делается

- **Пятый кейс.** Четыре — верное число; проблема не в количестве, а в разметке доказательств.
- **Смена визуального языка.** Утилитарный язык — соответствие позиционированию, а не недостаток. Он не будет доказательством для visual/consumer-ролей, и это принятая цена.
- **Сводный блок «60 секунд» сверху кейса.** В шапке уже стоит `outcome` — что решено и чем заплатили, одним абзацем (`CASE-02`). Ещё один сводный блок даст пятый объект в шапке рядом с `lead`, `meta`, `team` и `rework` и будет спорить с ним за то же место. Задача решается `L2-4`, а не дублированием.
- **Удаление технических чисел.** Они доказывают главный дифференциатор — доведение системы до кода. Правится позиция (`L2-2`), не наличие.

---

## Передача работы: что осталось и с чего начинать

Раздел для того, кто продолжает — человека или агента. Написан 2026-08-28, после закрытия всех `L1`, всех `L3` и четырёх `L2` из шести.

### С чего читать, по порядку

1. **Этот файл целиком** — таблицы выше и есть реестр. Строки с `~~зачёркиванием~~` закрыты, у каждой записано *чем именно* и какой ценой.
2. `PLAYBOOK-site-portfolio.md` §Чат 7 — правило уровней `L1` / `L2` / `L3`. Оно не про тяжесть находки, а про то, где живёт правка и кто решает.
3. `ds/CONTRACT.md`, затем `ds/foundation.md` и `ds/components.md` — обязательны перед любой правкой интерфейса. Это требование `CLAUDE.md`, не рекомендация.
4. `ds/screens/case-dssl.md` — самая полная карта кейса. Остальные три ссылаются на неё; `case-agent-ops.md` держит бюджет слов, `case-vet.md` — счёт по собранной странице и калибровку счётчика.

### Что осталось незакрытым

**`L2-5`, остаток кропов.** Единственный незакрытый пункт внутри репозитория. Замер, из которого всё следует: артефакт решения стоит в колонке `0.6fr` внутри `DecisionBlock`, то есть **~453 px** при контейнере 1232. Масштаб = 453 / ширина кадра. Ниже 0,3 подпись обещает то, чего не видно.

| Кейс | Кадр | Размер | Масштаб | Что делать |
|---|---|---|---|---|
| DSSL | `candidate-select.webp` | 2000×1250 | 0,23 | кроп |
| DSSL | `cart-change-review.webp` | 2000×1250 | 0,23 | кроп |
| DSSL | `nav-rail.webp` | 390×528 | 1,16 | **уже кроп, не трогать** |
| Vet | `dose-calculator.webp` | 2000×1960 | 0,23 | кроп |
| Vet | `patient-card-private.webp` | 2000×1947 | 0,23 | кроп |
| Vet | `discharge-preview.webp` | 2000×1692 | 0,23 | кроп |
| Vet | `visit-quick-trace.webp` | 1152×2375 | 0,39 | на границе, решить по кадру |
| Agent Ops | `autonomy.webp` | 2000×1250 | 0,23 | кроп |
| Agent Ops | `trace-gap-state.webp` | 2000×1927 | 0,23 | **кропом не лечится**, см. ниже |
| Pawly | все четыре | 564 px по ширине | 0,80 | **кропы не нужны** |

Два вывода, которые экономят время: **Pawly выпадает целиком** — это мобильный продукт, кадры узкие и читаются как есть; **`trace-gap-state` не кроп, а пересъёмка** — четыре состояния стоят в один ряд, на 453 px каждому достаётся 110 px, и любая граница режет сопоставление. Его надо пересобрать в две колонки в источнике (`Agent-ops-console`, Storybook), а не резать.

**`L2-4` снята, не отложена.** Указатель из заголовков стоил бы 71–97 слов при лимите, который существует против отказа от длинного чтения. Возвращать её без нового инструмента (бессловесная рельса) не нужно.

**`W-1`…`W-5` вне репозитория.** `W-3` (разговорный английский) и `W-5` (живой платный заказ с правом опубликовать показатель) агентом не закрываются вообще. `W-4` требует авторизованной сессии LinkedIn. `W-1` и `W-2` — презентации и ответы на шесть предсказуемых вопросов — написать можно, фактура для них в `src/copy/cases/` и `outputs/`.

**Одна несогласованность после `L3-7`.** `linkedin/change-set.md:191` всё ещё говорит `Professional working proficiency / B2`, тогда как в CV уровня больше нет. Профиль — живая публичная страница, правится только владельцем; файл пакета привести к CV нужно.

### Как делаются кропы

Инструмент готов: `scripts/crop-artifacts.mjs`, `sharp`, запуск `node scripts/crop-artifacts.mjs`. Добавляется запись `{ file, box, out? }`, координаты в системе исходного файла. Правила, выведенные из сделанных четырёх:

1. **Область берётся ровно та, о которой говорит подпись.** Если подпись говорит о двух вещах, а влезает одна — правится подпись, а не расширяется кроп. Кадр, который «показывает всё» и не читается, не показывает ничего.
2. **Граница проводится по промежутку, а не по содержимому.** Обрубок столбца или обрезанное слово читаются как сломанный скриншот. Обе сделанные пары переделывались по этой причине после просмотра.
3. **Кадр обязательно смотреть глазами до и после.** Коробка, посчитанная по описанию, ошибается.
4. **Оригинал не перезаписывается.** Кроп кладётся рядом с суффиксом `-crop`, оригинал уходит в `zoomSrc` у `MediaFrame` — на странице довод, в зуме весь экран.
5. **Русская пара обязательна.** `withRussianMedia` переписывает `/media/case-x` в `/media/case-x-ru`; кроп без пары даёт битую картинку на русской локали. Геометрия русских кадров зафиксирована по английским (`scripts/lock-ru-frame-geometry.mjs`), поэтому координаты те же — но длина подписей другая, и каждый русский кроп проверяется отдельно.

### Правила репозитория, которые дороже всего стоят при нарушении

- **Слова считаются до правки, а не после.** `CASE-15` — «бесплатное, но жёсткое» ограничение ~2500 слов. Agent Ops и Vet идут на границе. В `ds/screens/case-vet.md` §Бюджет слов записаны две несопоставимые бухгалтерии (по собранной странице против суммы блоков) и калибровка счётчика — читать до того, как добавлять текст.
- **Правка идёт в источник, зеркало обновляется тем же коммитом.** Для интерфейса — `ds/` → `src/`. Для текстов источник не `ds/`, а карта экрана `ds/screens/<name>.md`; `src/copy/` — зеркало, и у каждой строки есть русская пара.
- **Записанное обоснование нельзя обойти молча.** Сегодня дважды правка упиралась в записанный в коде запрет (ссылка на прототип, контакт в конце кейса). Оба раза верным было разобрать довод и переписать его на месте, а не проигнорировать. Иначе следующая правка обопрётся на запрет, которого больше нет.
- **Факты о владельце — всегда `L3`.** Даты, роли, уровень языка, что было проверено и на ком. Сверять с `cv/fact-sheet.md`; чего там нет — спрашивать, а не выводить.
- **Проверка перед отчётом.** `npm run build`, `npm run check:css`, затем грепом убедиться, что каждая ссылка на медиа разрешается на **обеих** локалях. Сегодня так поймалась битая русская локаль до того, как о работе было сказано «сделано».

### Чего делать не нужно

Пятый кейс. Смена визуального языка — утилитарность соответствует позиционированию. Сводный блок «60 секунд» сверху кейса — в шапке уже стоит `outcome`. Удаление технических чисел — правится их позиция, не наличие.
