# Screen: Agent Ops Console case

**Маршрут:** `/work/agent-ops-console` · шаблон `src/pages/work/[slug].astro`
**Тексты:** `src/copy/cases/agent-ops-console.ts`
**Медиа:** `public/media/case-agent-ops/` · съёмка `scripts/shoot-agent-ops-frames.mjs`
**Источник фактуры:** `d:\Claude-projects\Agent-ops-console`
**Статус:** собран 2026-08-27 из принятого шаблона кейса

Четвёртый экземпляр шаблона, зафиксированного в `ds/screens/case-dssl.md`.
Порядок секций, роли компонентов, ритм, адаптив и движение не меняются.
Новый предмет карты — содержание, доказательства, производство кадров тёмного
десктопного продукта и **перестановка порядка кейсов на главной**.

---

## Тезис

**Надзор как механизм автономии, а не как список запретов.** Кейс показывает не
«дашборд для AI», а смену единицы контроля: проверять не сообщения, а
обязательства — возвраты, кредиты, изменения тарифа, обещания срока. Полная
проверка 1 770 диалогов в день недостижима (25,3 FTE против команды в 24
человека); проверка 100 % обязательств — выполнима.

Ограничение формулируется в шапке и в результате одинаково: работа доведена до
протестированного прототипа, принятого заказчиком. Дальнейшая реализация — на
стороне клиента, поэтому post-launch показателей в кейсе нет и быть не может.

---

## Composition

Шаблон один в один с тремя опубликованными кейсами:

1. `CaseHeader` — `SectionHead` + lead `ProseBlock` + inline `MetaList` +
   outcome `NoteBlock emphasis` + team `MetaList stacked` + оговорка
   `NoteBlock flagged`.
2. `CaseCover` — `ScreenStack` из ровно трёх кадров.
3. `CaseContext` — успешные метрики, скрывающие цену ошибок внутри них.
4. `CaseReframe` — единица проверки меняется с сообщения на обязательство;
   закрывается кадром диапазона ширин (`range`, добавлен 31.08.2026).
5. `CaseProcess` — интервью → аудит → конкуренты → ролевая модель → ДС →
   прототип → юзер-тест; ссылка на живой прототип, клип взаимодействия
   (`clip`, добавлен 31.08.2026) и два артефакта.
6. `CaseFailure` — три дефекта собственной сборки: цифра на чипе вердикта,
   потерянная телеметрия и ролевая модель, прочитанная не по требованию.
7. `CaseDecisions` — четыре `DecisionBlock`: очередь по экспозиции и кластер
   причины; вес пробела в трейсе; перехват денег до исполнения; лестница
   автономии.
8. `CaseSystemGrid` — шесть матриц `AllVariants` из Storybook.
9. `CaseResult` — что решено, чем заплачено, что проверено и чего в кейсе нет.
10. `CaseOutro` — переход к следующему кейсу (`L2-3`, 2026-08-28).

Новых компонентов, паттернов, токенов и движений экран не добавляет.

---

## Факты, которые нельзя переписывать свободно

Разделены на три класса, и класс определяет формулировку.

**Строка `Evidence` в мете шапки** — `Paid client · user-tested · accepted`, добавлена 2026-08-28 (находка `L2-1`, канонический разбор — `ds/screens/home.md` §FeaturedCase). Каждое из трёх слов держится фактом: заказ платный, прототип тестировали на живых людях и дорабатывали по находкам, заказчик работу принял. Слова `shipped` в строке нет и не появляется — сборка была на стороне клиента, и правило 1 этой карты запрещает любую заявку на результат после запуска. Строка дословно совпадает с обложкой на главной.

**Операционные факты заказчика** — разрешены к публикации, идут как факты:
61 % обращений закрывалось агентом, CSAT 4,4, апрельский инцидент на **$23 000**
(четырнадцать клиентов, промоакция, закончившаяся двумя годами ранее, найдено
примерно через 19 дней), 24 сотрудника в трёх часовых поясах, около **1 770**
автоматически закрытых диалогов в день, около **40 000** компаний-клиентов.

**Проектные оценки** — называются оценкой, не измерением: полная проверка ≈ 177
часов в день ≈ **25,3 FTE**; обязательства ≈ **4 %** потока ≈ **71** карточка в
день ≈ 2,4 часа ≈ **0,3 FTE**. Формулировка «by my estimate» обязательна.

**Факты сборки** — проверяемы по репозиторию:
- **Система:** 88 primitive + 70 semantic токенов, 17 TextStyles, 42 компонента,
  331 вариант. Оба режима заполнены, `Dark` основной.
- **Объём:** 35 артбордов в scope отрисовки, 18 собрано, 19 экранов и
  20 маршрутов в прототипе.
- **Сверка с макетом:** 67 находок, 67 закрыто вторым проходом (`audit/
  screens-parity-2026-08-27.md`).
- **Агентский QA:** раунд 1 — **6 провалов из 42**, раунд 2 — **0 из 41**,
  регрессий нет (`audit/agent-qa-2026-08-27.md`).

**Чего в кейсе нет и не появится:** ни одной post-launch метрики, ни одного
утверждения о сокращении потерь как об измеренном результате, ни имени
заказчика, вендора и участников исследования. Имена и компании на кадрах
(`Nordwind GmbH`, `Helio Retail`, `Martin K.`) — мок-данные прототипа.

**Агентский QA не переводит ни одну гипотезу в `validated`** и в кейсе не
выдаётся за пользовательскую валидацию: он снимает то, что снимается без людей.
Живая проверка прототипа с пользователями — отдельный факт и названа отдельно.

---

**Строка `Собрано` снята из `CaseResult`, на её место встала `Пожертвовано`. Правка 2026-08-28, находка `L2-2`.**

Инвентарь дизайн-системы в этой строке был вторым носителем: те же 42 компонента, 331 вариант, 88 примитивных и 70 семантических токенов уже стоят абзацем выше, в `CaseSystem`, где рядом с ними есть матрицы и смысл. В `CaseResult` они занимали позицию расплаты — место, где читатель ждёт, чем решение обошлось, а получал опись сборки. Дублирование снято удалением, а не переносом: переносить было некуда, всё уже сказано.

**Освободившаяся строка отдана цене, и это возврат к схеме, а не новизна.** У DSSL и Vet Clinic OS `CaseResult` идёт `Решено · Пожертвовано · Почему цена оправданна · Что изменилось`. Здесь и в Pawly две средние строки были вытеснены описью и QA — то есть именно те два кейса, что потеряли формулировку цены, приобрели счётчики. Цена в шапке стояла (`outcome`), в расплате — нет.

Схема при этом не выравнивается под DSSL до конца: четвёртой строкой остаётся `Проверено`, а не `Почему цена оправданна`. У этого кейса проверка — предмет доказательства, а не служебная деталь: живой юзер-тест и приёмка заказчиком есть только здесь. Строка переписана так, чтобы первым шло человеческое подтверждение, а счётчики прогона — контекстом за ним. Числа, снятые отсюда: 35 артбордов, 18 собранных, 67 находок parity-проверки.

## Производство кадров тёмного десктопного продукта

Норма отличается от трёх предыдущих кейсов **одним числом**, и отличие
вынужденное. DSSL, Vet Clinic OS и Pawly снимались `1440×900` — ровно 16:10,
кроп не нужен. Agent Ops спроектирован под `1440×1024` с фиксированной высотой
артборда: скролл живёт внутри колонок, нижние полосы действий прибиты
`position: sticky`. На 900 последняя строка таблицы режется пополам —
проверено на `review-queue` 2026-08-27.

Поэтому viewport `1640×1025`: те же 16:10 (1640/1025 = 1,6), но высота вмещает
артборд целиком. Ширина уходит в поля полосы контента — оболочка консоли
текучая. Выход `2000×1250 WebP q82`, `deviceScaleFactor` 1.5, обработка только
даунскейл и кодирование. Ни рамок, ни перспективы, ни наклона, ни тени, ни
цветокоррекции (`CASE-20`).

**Два кадра сняты выше нормы, и это следствие того же правила, а не отступление
от него.** `cluster-detail` (таблица на 17 строк) и `trace-gap-state` (сравнение
четырёх весов пробела) в 1025 не помещаются: на нижнем краю остаётся строка,
разрезанная прилипшим футером, и половина второй пары карточек. Высоты **1060**
и **1580** сняты замером `scrollHeight` области контента — это минимум, при
котором прокрутки внутри экрана не остаётся вовсе. Оба кадра идут `MediaFrame
ratio=natural` и в стопку обложки не попадают, поэтому 16:10 им не нужно.

**Роль в `TopStrip` — часть кадра, а не декорация.** Очередь подтверждений
принадлежит Shift Lead, автономия и качество — Policy Owner. Роль ставится
ссылкой на индексе прототипа, дальше маршрут открывается внутри SPA
(`pushState` + `popstate`): бэкенда нет, состояние живёт в памяти, и
перезагрузка начала бы смену заново вместе с ролью.

Матрицы системы снимаются из Storybook `AllVariants` и вручную не собираются.
Ширина колонки каталога у каждой истории своя: 760 общая, **1160** у `VerdictBar`
и `AutonomyLadder` — на 760 первая теряла кнопку `Submit`, вторая — колонку
уровня справа. Одно число на все ячейки стоило бы обрезанного варианта в
доказательстве матрицы состояний. Обрезки по содержимому нет: `.storybook-canvas`
несёт своё поле, а `trim()` на тёмном фоне съел бы его неравномерно.

Скрипт прекращает съёмку, если находит кириллицу в кадре, — та же защита, что в
`shoot-pawly-frames.mjs`, и по той же причине: поймать русский fixture глазами на
девятнадцати экранах нельзя.

---

**Артефакты решений режутся под колонку, в которой стоят. Правка 2026-08-28, находка `L2-5`.**

Замер, из которого всё следует: `DecisionBlock` кладёт артефакт в колонку `0.6fr`, и при контейнере 1232 px с жёлобом 24 это **~453 px**, а не ширина кейса. Полный кадр продукта — 2000 px — показывался там в масштабе 0,23: подпись обещала «$4,200» и «one expired article», а прочитать их было нельзя. Три кропа дают 0,53, 0,40 и 0,45.

**Что рассматривалось вместо кропа.** Отдать артефакту всю ширину колонки кейса: масштаб 0,62, резать нечего. Отклонено — четыре решения в каждом из четырёх кейсов добавили бы порядка 2 800 px к странице, которая и так идёт около 11 000. Кроп сохраняет плотность, а полный экран открывается в зуме через новый проп `zoomSrc` у `MediaFrame`.

**Подпись правится вместе с кадром, а не остаётся шире него.** У кластера в кроп не влезла таблица участников, и обещание «members stay visible and removable» из подписи снято — то же утверждение стоит в `cost` этого решения, где ему и место. Кадр, который «показывает всё» и не читается, не показывает ничего.

**`autonomy` режется до одного честного довода.** Семь способностей, пять уровней и колонка доказательств разнесены по всей ширине; сохранить их все значило бы оставить масштаб 0,23. Кроп `1010×180` показывает заголовок лестницы и автоматическое понижение возврата с L3 до L2 после дефекта тяжести 1. Подпись больше не обещает семь читаемых лестниц и доказательства каждой позиции; полный экран остаётся в `zoomSrc`.

**Один кадр из четырёх остаётся полным намеренно.** `trace-gap-state` — сопоставление четырёх состояний в один ряд; на 453 px каждое получает по 110 px, и кроп тут не поможет: нужна пересъёмка в две колонки в источнике. Резать отдельные состояния значит уничтожить само сравнение.

**Русская пара обязательна.** `withRussianMedia` переписывает `/media/case-agent-ops` в `/media/case-agent-ops-ru`, и кроп без русского близнеца даёт битую картинку на русской локали. Поймано сборкой до отчёта. Геометрия русских кадров зафиксирована по английским (`scripts/lock-ru-frame-geometry.mjs`), поэтому координаты те же — но каждый русский кроп проверен глазами отдельно: размеры совпадают, длина подписей нет.

## Что этот экран меняет на главной

**Порядок кейсов переставлен. Решение владельца 2026-08-27.** Было DSSL → Vet
Clinic OS → Pawly, стало **Agent Ops Console → DSSL → Vet Clinic OS → Pawly**.

Обоснование — убывание доказательной силы, а не хронология и не вкус. Agent Ops
единственный из четырёх прошёл полный цикл на живом заказе: платный клиент,
живые интервью, юзер-тест прототипа и приёмка заказчиком. DSSL — коммерческий
редизайн без пользовательской проверки в этой редакции. Vet Clinic OS —
концепт с одним живым врачом. Pawly — концепт целиком. Первым стоит тот кейс,
который отвечает на самый дорогой вопрос найма — «работал ли он с реальными
пользователями и реальной ценой ошибки» — до того, как читатель дойдёт до
второго экрана.

Второй довод — отстройка. Три открытых кейса светлые; Agent Ops тёмный. Обложка
первого экрана перестаёт быть похожей на предыдущую.

Прочие следствия:

- `home.featured.items` получает четвёртый экземпляр `FeaturedCaseCover`,
  вставленный **первым**, а не дописанный снизу.
- `loading="eager"` в `index.astro` привязан к `index === 0` и уезжает на новую
  первую обложку сам; разметку менять не требуется.
- Та же тройка кадров стоит в `home.ts` и в `agent-ops-console.ts`: обложка на
  главной и `CaseCover` — один объект, а не два кадрирования.
- Реестр `src/copy/cases/index.ts` меняет порядок вместе с главной: из него
  разворачиваются `sitemap.xml`, `hreflang` и OG-карточки, и расхождение
  порядка между реестром и главной было бы расхождением между картой сайта и
  сайтом.

**Секция `WorksList` снята с главной целиком. Решение владельца 2026-08-27.**
В ней оставалась одна запись — RUUN, «case is not written yet». Кейс по RUUN
писать не будем: решение владельца, проект из портфолио убирается. С уходом
единственной записи секция теряет предмет, поэтому снимаются и заголовок, и
подводка, и `home.works`, и разметка секции в `index.astro`. Компонент
`WorkRow` после этого не имеет носителей — он остаётся в каталоге и в `/kit`,
но со страниц продукта уходит; заводить пустую секцию ради компонента нельзя
(`TECH-15`).

---

## Бюджет слов (`CASE-15`, лимит ~2500)

Подсчёт по строкам файла текстов (включая `alt`, которые на странице не
печатаются). Контрольное число — **2 488 слов в `dist/work/agent-ops-console/
index.html`**, то есть по видимому тексту вместе с оболочкой страницы. Для
сравнения: DSSL 2 101, Vet Clinic OS 2 427, Pawly 2 296.

| Блок | Слов |
|---|---|
| CaseHeader | 187 |
| CaseCover | 55 |
| CaseContext | 190 |
| CaseReframe | 230 |
| CaseProcess | 254 |
| CaseFailure | 253 |
| CaseDecisions | 678 |
| CaseSystem | 285 |
| CaseResult | 301 |
| CaseOutro | 44 |
| **Итого** | **2477** |

**Первая редакция дала 2 727 и в лимит не прошла**: `CaseDecisions` весил 762
против бюджета 520. Ужималась вся страница, а не один блок. Резать только
решения значило бы получить четыре тезиса без обоснования рядом с
полновесными соседними секциями — а обоснование и цена в `DecisionBlock` и
есть предмет доказательства (`CASE-09`).

`CaseDecisions` остаётся самым тяжёлым блоком страницы (678 против 613 у Vet
Clinic OS) по устройству кейса: у всех четырёх решений заполнен слот
артефакта, тогда как у DSSL один из четырёх идёт без него. Пятое решение не
берётся ни при каком запасе.

**Пересчёт 31.08.2026, строка `Platform`.** `Web, desktop-first` заменено на
`Web — desktop-first, responsive to 360 px`: три прототипа доведены до полного
адаптива 360–1440, а сайт продолжал называть их desktop-first — читатель
проверяет это за пять секунд по живой ссылке. Цена **+5 слов** к странице,
взяты из записанного запаса 23. Замер сделан третьим счётчиком (по `<main>`
собранной страницы, без навигации и подвала): 2 521 → 2 526. Его нельзя
сравнивать ни с 2 477 этой таблицы, ни с 2 499 по странице в
`case-vet.md` — метод описан в `audit/visual-density-2026-08-31.md` §1,
и вердикт по `CASE-15` по нему не выносится. Разбор целиком — там же.

**Клип взаимодействия в `CaseProcess`, 31.08.2026 — открытый пункт.**
Подпись ролика стоит **+23 слова** по-английски и **+19** по-русски. Запас
кейса был израсходован полностью кадром диапазона в тот же день (ниже), то
есть эта правка нарушает записанное там же правило «следующая правка обязана
начинаться с высвобождения слов».

Два счётчика расходятся, и вердикт `CASE-15` зависит от того, какой считать
верным: по видимому тексту `<main>` собранной страницы кейс идёт **2 489**
при лимите ~2 500, то есть в пределах; по третьему счётчику раздела ниже —
**2 567**, то есть за ним. Метод третьего счётчика описан в
`audit/visual-density-2026-08-31.md` §1 и считает больше, чем видимый текст.

**Решение владельца от 31.08.2026: оставить как есть.** Единственной
бухгалтерией `CASE-15` признан **видимый текст `<main>` собранной страницы** —
то, что читатель действительно читает. По нему кейс идёт 2 489 при лимите
~2 500 и в пределы укладывается; подпись ролика остаётся целиком.

Третий счётчик остаётся справочным и вердикта по `CASE-15` не выносит.
Отклонены: сокращение подписи до первой фразы (−10 слов) — потеряло бы окно
отзыва, единственное, что подпись добавляет к видимому в кадре; и
высвобождение 23 слов из прозы кейса — редактура выверенного текста ради
расхождения счётчиков, а не ради читателя.

**Кадр диапазона в `CaseReframe`, 31.08.2026.** Подпись стоит **+18 слов**
(2 526 → 2 544 третьим счётчиком; от базовой линии 2 521 это +23 — ровно
записанный выше запас, и он теперь израсходован полностью). **Следующая
правка текста этого кейса обязана начинаться с высвобождения слов.**

Плата взята сознательно: кадр разрывает самый длинный текстовый прогон
страницы. Было 740 слов от подписи обложки до первого артефакта, стало
**449 и 309**, и самым длинным прогоном страницы теперь стал соседний —
449 против прежних 740. Композит вместо трёх кадров в вёрстке и место
именно в конце `CaseReframe` — `audit/visual-density-2026-08-31.md` §9.

---

## Tokens used

Те же смысловые токены, что у трёх опубликованных кейсов. Своих значений экран
не заводит: `--text-default / subtle / muted / meta`, `--surface-default /
subtle / media`, `--border-default / strong`, роли типографики
`ds-heading-3xl / 2xl / xl`, `ds-body-lg / base`, `ds-meta-sm / xs`, меры
`--measure-text` и `--measure-lead`, вертикаль через `.ds-section`.

**Тёмные кадры не меняют палитру страницы.** Кейс светлый, как и остальные три;
тёмное — содержимое `MediaFrame` и `ScreenStack`. `--surface-media` под кадром
остаётся прежним: подложка видна только на времени загрузки и на полях кадра,
и подгонять её под цвет продукта значило бы завести четвёртый носитель темы
ради одного маршрута.

## Иконки

Иконок нет. Стрелка `TextLink type=arrow` — символ в разметке компонента.

---

## Приёмка

- `/work/agent-ops-console` генерируется реестром `src/copy/cases/index.ts`, без
  ветки в шаблоне.
- OG-карточка, `sitemap.xml` и `hreflang` приходят из того же реестра.
- Объём ≤ 2500 слов — подсчёт по `dist/work/agent-ops-console/index.html`.
- Каждый кадр просмотрен глазами на предмет обрезки: ни одной строки, кнопки,
  полосы и карточки, разрезанной краем кадра.
- Ни одной кириллической строки в кадрах — гейт в скрипте съёмки.
- Ни одной post-launch метрики и ни одного реального имени — пословный просмотр
  `CaseResult` и всех кадров.
