# Screen: Pawly case

**Маршрут:** `/work/pawly` · шаблон `src/pages/work/[slug].astro`
**Тексты:** `src/copy/cases/pawly.ts`
**Медиа:** `public/media/case-pawly/`
**Источник фактуры:** `d:\Claude-projects\PETS-walking`
**Статус:** собран локально 2026-08-25 из принятого шаблона кейса

Pawly не заводит новую страницу-композицию: это третий экземпляр шаблона,
зафиксированного в `ds/screens/case-dssl.md` и проверенного вторым носителем
в `ds/screens/case-vet.md`. Порядок секций, роли компонентов, ритм,
адаптив и движение не меняются. Новый предмет карты — только содержание,
доказательства и производство кадров мобильного продукта.

---

## Тезис

**Trust as evidence, not a badge.** Кейс показывает не маркетплейс «Uber
для собак» как набор счастливых экранов, а сервисную модель, где доверие
разложено на проверяемые факты: зона и совместимость до бронирования,
семь этапов верификации с датами, фото передачи и возврата, live-route с
офлайн-буферизацией и совместимая замена, если выбранный исполнитель отменил.

Ограничение формулируется в шапке и результате одинаково: это интерактивный
концепт, не запущенный сервис. Живых интервью, usability-сессий, заказов,
выручки, конверсии и retention нет. Синтетический прогон — свидетельство о
поведении прототипа, не о спросе или adoption.

---

## Composition

Шаблон остаётся один в один с двумя опубликованными кейсами:

1. `CaseHeader` — `SectionHead` + lead `ProseBlock` + inline `MetaList` +
   outcome `NoteBlock emphasis` + team `MetaList stacked` + оговорка
   `NoteBlock flagged`.
2. `CaseCover` — `ScreenStack` из ровно трёх кадров.
3. `CaseContext` — проблема передачи собаки и иногда ключей незнакомому
   человеку в короткое окно рабочего дня.
4. `CaseReframe` — доверие как цепочка доказательств, а не бейдж;
   закрывается кадром цепочки (`range`, добавлен 31.08.2026).
5. `CaseProcess` — research → scope → IA → system → prototype → QA;
   ссылка на живой прототип, клип взаимодействия (`clip`, добавлен
   31.08.2026) и два артефакта.
6. `CaseFailure` — рассинхрон цены, протекающее состояние между сессиями и
   интерактивные контролы внутри ссылок галереи: ошибки, которых нет в кадре.
7. `CaseDecisions` — четыре `DecisionBlock`: ранняя проверка зоны;
   верификация с датами; фото как граница услуги; совместимая замена.
   Артефакты второго и четвёртого — клипы, а не снимки (31.08.2026).
8. `CaseSystemGrid` — шесть матриц `AllVariants` из Storybook.
9. `CaseResult` — что решено, чем заплачено, что проверено и чего пока нет.
10. `CaseOutro` — переход к следующему кейсу; последний замыкается на первый (`L2-3`, 2026-08-28).

Новых компонентов, паттернов, токенов и движений экран не добавляет.

---

## Факты, которые нельзя переписывать свободно

- **Строка `Evidence` в мете шапки** — `Independent concept · no human validation`,
  добавлена 2026-08-28 (находка `L2-1`, канонический разбор —
  `ds/screens/home.md` §FeaturedCase). Отсутствие живой проверки названо первым,
  а не последним: строка обязана снимать вопрос до входа в кейс, а не после.
  Дословно совпадает с обложкой на главной.
- **Объём:** 95 экранов в sitemap, 81 core, семь flow. Точная строка кейса:
  **81 designed, 16 assembled**. В React это 17 routed frames/states плюс
  responsive landing.
- **Система:** 63 primitive + 30 semantic tokens, 14 text styles,
  33 React components.
- **QA:** 58 находок основного/consistency review + 15 visual follow-ups;
  71 закрыта, две сняты как ложные. Финальная приёмка — 91 скриншот,
  девять отчётов, ноль console errors на 17 маршрутах.
- **Agent QA:** synthetic. 5/5 верных первых действий, 3/3 happy path;
  major-дефект persistence найден, исправлен и прошёл retest.
- **Метрики PRD** — будущие цели. Weekly bookings, fill rate и proof coverage
  не стоят ни в outcome, ни в result как достигнутые.
- **Simulated interview** остаётся симуляцией во всех упоминаниях.

---

**Строка `Собрано` снята из `CaseResult`, на её место встала `Пожертвовано`. Правка 2026-08-28, находка `L2-2`.** Разбор целиком — `ds/screens/case-agent-ops.md`: правка одна на два кейса. Здесь то же самое — 33 компонента и семантика уже названы в `CaseSystem`, а позиция расплаты стояла под описью сборки. Цена у концепта своя и названа своими словами: узкий запуск, отказ от страховки и гарантированной замены, ручные операции за интерфейсом. Числа, снятые отсюда: 81 узел, 16 собранных экранов, 91 скриншот, девять отчётов, ноль ошибок консоли. В `Проверено` остался дефект, который имел значение, — состояние одного посетителя доживало до следующего.

## Обложка мобильного продукта

`ScreenStack` держит `ratio-screen` 16:10 и `object-fit: cover`. Сырой экран
Pawly имеет ширину 375 px и высоту 656–1928 px; положенный напрямую, он будет
обрезан почти целиком. Поэтому `scripts/shoot-pawly-frames.mjs` производит
три широких кадра одним прогоном:

1. индекс первых экранов `/app` — передний кадр, где продукт считывается до
   текста кейса;
2. owner home в нейтральном поле viewer без служебной шапки;
3. review возвратного фото в том же поле.

Норма совпадает с двумя предыдущими кейсами: `1440×900` CSS,
`deviceScaleFactor: 1.5`, выход `2000×1250 WebP q82`, без перспективы,
наклона, внешней рамки, тени и цветокоррекции. Скругление и обводку даёт
только `ScreenStack`. Полные мобильные экраны ниже снимаются в собственной
высоте. Служебная фиксированная шапка viewer скрывается до element screenshot,
поэтому в файл попадает только `.device`; в решениях кадры идут через
`MediaFrame ratio=natural rounded fill`, заполняют правую колонку и сохраняют
скругление карточки. Индекс и матрицы остаются обычным `natural`.

Матрицы системы снимаются из Storybook `AllVariants`, обрезаются по
содержимому с полем 24 px и не собираются вручную. Все видимые строки этих
stories — английские: `Button`, `InfoNote`, `EmptyState`, `BottomSheet`,
`PhotoProof`, `TimelineRow`, `IconButton`. Производственный скрипт прекращает
съёмку, если находит кириллицу в `#storybook-root`, чтобы русский fixture не
мог незаметно вернуться в опубликованный кейс.

---

## Кадр цепочки, 31.08.2026

В конце `CaseReframe` встал `range` — один заказ в трёх моментах на 375 px:
проверка заказа, идущая прогулка, завершённая прогулка
(`scripts/shoot-range-frames.mjs pawly`, маршруты `/app/booking-review`,
`/app/active-service`, `/app/order-details`).

**Поле называется `range`, как у трёх других кейсов, но диапазон здесь
другой.** У Agent Ops, DSSL и Vet Clinic OS колонки — один экран на разных
ширинах; продукт там десктопный, и доказывать нужно адаптив. Pawly мобильный,
375 px — каноническая ширина его экранов, и диапазон ширин доказывал бы то, чего продукт не
обещает. Диапазон идёт по времени сервиса, и три момента взяты не произвольно:
это ровно те три, что названы в лиде кейса — до передачи собаки, во время
прогулки, после возвращения.

**Три экрана выбраны так, чтобы не повторить ничего на странице.**
`address-input`, `walker-profile`, `handover-photo-review` и
`replacement-offer` уже стоят артефактами решений, а `screen-index` — это
нижняя часть того же кадра галереи, чья верхняя часть лежит в обложке. Из
семнадцати маршрутов прототипа свободны и содержательны именно эти три.

**Высота у колонок разная — единственный такой кадр из четырёх.** Там, где
колонка это один экран на разных ширинах, общая высота обязательна, иначе
кадр сравнивает не то. Здесь экраны разные, и `booking-review` на треть
короче двух других: общая высота 1200 дорисовала бы под ним пустое белое
поле. Колонке дана своя — 980, а не 900, потому что по-русски дисклеймеры
выше и на 900 обрезалась строка удержания средств под кнопкой оплаты.

**Ширина и углы колонок нормализуются в производственном скрипте.** Viewer,
`.device` и экран снимаются в одинаковые 375 px: отдельного 390 px viewport,
который оставлял 15 px пустого поля справа от продукта, больше нет. После съёмки композитор
возвращает каждой колонке `radius-lg` и только затем ставит их на общую
подложку с одинаковым промежутком.

**Цена и что она купила.** Подпись — **+21 слово**: 2 341 → 2 362 по счётчику
`audit/visual-density-2026-08-31.md` §1 (по `<main>` собранной страницы;
с двумя другими бухгалтериями репозитория он не сверяется, и вердикт по
`CASE-15` по нему не выносится). Самый длинный текстовый прогон страницы:
**679 → 419**, разрыв разошёлся на 419 и 281. До правки 679 было худшим
показателем сайта, после — 419 против 449 у Agent Ops, 475 у Vet Clinic OS и
351 у DSSL.

**Чего здесь сознательно не сделано.** `screen-index` не поднят выше, хотя
это стоило бы ноль слов и дало бы 218 / 201 / 284. Он повторяет передний кадр
обложки, и его подпись аргументирует дисциплину scope — довод, который
рассказывается только в `CaseProcess`, абзацем ниже места переноса.

## Клипы взаимодействия, 31.08.2026

Три ролика с живого прототипа — `node scripts/shoot-clips.mjs pawly`, обе
локали одним прогоном. Ширина съёмки 375 px — каноническая ширина экранов
продукта; высота у каждого своя и равна высоте экрана в реестре прототипа,
чтобы в кадре не оставалось пустой подложки.

| Ролик | Место | Экран | Длительность |
|---|---|---|---|
| `clip-booking-disclosures` | `CaseProcess`, за ссылкой на прототип | `/app/booking-review` | 10,8 с en · 10,8 с ru |
| `clip-verification` | `CaseDecisions` 2 | `/app/walker-profile` | 7,6 с en · 7,7 с ru |
| `clip-replacement` | `CaseDecisions` 4 | `/app/replacement-offer` | 10,6 с en · 11,0 с ru |

**Три, а не один, потому что кейс обещает три разные вещи.** Что сказано до
оплаты; что стоит за словом «проверен»; что происходит, когда выгульщик
отменился. Первое — общее и стоит в `CaseProcess`; два других — доказательства
конкретных решений и стоят их артефактами.

**Два ролика заменили снимки, и оба заменили их не ради движения.** На
`walker-profile` и `replacement-offer` главного просто нет в кадре: шторка «как
мы проверяем» открывается только нажатием, а обратный отсчёт на кнопке
подтверждения — единственное во всём портфолио, чего снимок не может показать
в принципе. Файлы `walker-profile.webp` и `replacement-offer.webp` с русскими
парами удалены, записи съёмки сняты из `scripts/shoot-pawly-frames.mjs`.

**Указатель — круг касания, а не стрелка.** Продукт мобильный, и стрелка
показывала бы взаимодействие, которого у него не бывает. Реализация — там же,
где стрелка: узел поверх страницы, который ведёт сама страница подпиской на
настоящую мышь Playwright, поэтому нажатие и то, что от него происходит, не
могут разойтись.

**Петли закрываются состоянием, а не монтажом.** Шторки закрываются своими же
кнопками, замена уходит по «выбрать другого» и возвращается назад — отсчёт при
этом стартует заново с монтированием экрана. Оплата в кадр не входит: концепт
ничего не проводит, и ролик не должен обещать обратное.

**Плотность съёмки — 1.5, как у кадров.** Ролик, снятый один к одному,
вставал бы в колонку шириной 250 px вместо 375: `src/lib/media.ts` делит
пиксели файла на плотность производства кадров, и телефон получился бы мельче
собственных снимков на той же странице, а в слоте с `fill` ещё и растянулся бы
против исходника. Разрешения добавляет не настройка записи — `Page.screencast`
отдаёт кадр размером с окно в CSS-пикселях и умеет только уменьшать, — а само
окно: viewport берётся в полтора раза больше и страница масштабируется `zoom`
на ту же величину. Раскладка при этом не меняется: продукт по-прежнему
верстается под 375 px (`innerWidth` 562, `body.scrollWidth` 375).

**Цена в словах — плюс 20** (2 321 → 2 341 по видимому тексту `<main>`, лимит
~2 500). Новая подпись в `CaseProcess` стоит +22, а две подписи решений стали
короче на 2: у второго решения ушло перечисление того, что и так видно в
кадре, у четвёртого — фраза про «не пересобирать заказ», вместо которой
подпись теперь называет идущий отсчёт.

**Правка в прототипе, сделанная съёмкой.** Обе шторки раскрытий не были
переведены в русской ветке `PETS-walking`: они открываются только нажатием, а
кадров с них не снимали. Добавлена 21 фраза в
`src/i18n/RussianLocalization.tsx`; отдельной записью — суффикс `s` на кнопке
обратного отсчёта, который в русском кадре читался латиницей («57s» вместо
«57 с»).

---

## Что экран меняет на главной

- `home.featured.items` получает третий экземпляр `FeaturedCaseCover` после
  Vet Clinic OS; порядок DSSL → Vet Clinic OS → Pawly.
- Та же тройка кадров стоит в `home.ts` и в `pawly.ts`: обложка на главной и
  `CaseCover` — один объект.
- Pawly удаляется из некликабельного `WorksList`; там остаётся RUUN.
- Заголовок `Two more projects…` становится `One more project…`, lead тоже
  переходит в единственное число.
- `loading="eager"` остаётся только у DSSL. Vet Clinic OS и Pawly ленивые;
  разметку `index.astro` менять не требуется.

---

## Приёмка

- `/work/pawly` генерируется реестром `src/copy/cases/index.ts`, без ветки в
  шаблоне.
- OG-карточка и sitemap приходят из того же реестра автоматически.
- `scripts/check-scoped-css.cjs` включает `dist/work/pawly/index.html`.
- Проверки: `npm run check`, `npm run build`, `npm run check:css`.
- Визуальный проход: главная и кейс на desktop и 360/375; обложка, natural
  media, system grid, zoom, отсутствие горизонтальной прокрутки.
- `node scripts/verify-portfolio-locales.mjs` против `npm run preview --
  --host 127.0.0.1`: обе локали, четыре ширины, кадры и консоль.
