# Screen: CaseVet

**Маршрут:** `/work/vet-clinic` · файлы `src/pages/work/[slug].astro` + `src/copy/cases/vet-clinic.ts`
**Источник:** `ds/screens/case-dssl.md` — шаблон страницы кейса, здесь не переизобретается · `outputs/prd.md` US-07…US-11, US-18, US-19
**Оболочка:** `PageShell` — `Navbar` + содержимое + `Footer`
**Материал кейса:** `d:\Claude-projects\Veterinary-clinic` — `CLAUDE.md`, `outputs/brief.md`, `outputs/interview_primary_persona.md`, `outputs/mvp_scope.md`, `ds/foundation.md`, `ds/components.md`, `ds/screens/_index.md`, история коммитов (QA-волны 1–4). Репозиторий `github.com/NikitaKanarev97/veterinary-clinic`
**Прототип:** `https://veterinary-clinic-gules.vercel.app/` — живой, без логина, проверен 2026-08-25
**Статус:** собран 2026-08-25. Карта согласована владельцем до вёрстки, кадры сняты с локальной сборки прототипа, приёмка пройдена — таблица внизу

**Те же три ограничения, что у `CaseDSSL`** — страница самодостаточна как точка входа (`CASE-17`), шапка читается за 5 секунд (`US-07`), объём ≤ ~2500 слов (`CASE-15`). Заголовки — утверждения (`CASE-14`).

**Маршрут и вёрстка не трогаются.** Второй кейс — это ровно то, ради чего `src/pages/work/[slug].astro` собран шаблоном с первого кейса (`US-20`, `IA-02`): новый файл в `src/copy/cases/` плюс строка в реестре. Если для этого кейса понадобится новый блок — это правка шаблона, и она обсуждается отдельно, а не проносится под видом второго кейса.

---

## Решения карты

### 1. Факты шапки — решение владельца 2026-08-25

Артефакты проекта `Veterinary-clinic` в трёх местах (`CLAUDE.md`, `outputs/brief.md`, `outputs/interview_primary_persona.md`) заявляют формат «продуктовый кейс без живого заказчика, интервью — симуляция на вторичных данных». **Владелец 2026-08-25 сообщил обратное:** заказчик был реальный — частная ветклиника, работа под NDA, кейс пересобран; интервью — несколько живых разговоров со знакомым практикующим ветврачом.

Расхождение закрывается так:

- **На странице пишется версия владельца**, потому что это его факты о его работе, а артефакты писались под другой формат подачи.
- **Ни одна цифра и ни один факт не додумываются.** Прямая просьба «остальные данные можешь сам додумать» (2026-08-25) не исполняется: страница для найма — набор проверяемых утверждений, и выдуманный состав команды вскрывается первым уточняющим вопросом на собеседовании. Чего нет — того на странице нет.
- Закрытые фактуры: **Team** — единственный дизайнер, больше никого; **Research** — один практикующий ветврач, несколько разговоров, без формального гайда; **судьба продукта** — дошло до дизайна и прототипа, в боевую разработку не пошло; **Year** — 2026; **Duration** — две недели от брифа до работающего прототипа (подтверждается датами артефактов 11–25 августа 2026 и историей коммитов).
- **Расхождение в самом репозитории `Veterinary-clinic` остаётся незакрытым** и вынесено в §«Осталось открытым». Правка чужого проекта в объём этой задачи не входит, но пока она не сделана, публичная страница и репозиторий говорят разное.

### 2. Оговорка шапки — `NoteBlock variant=flagged`, как у DSSL

Носитель и место те же: последней в шапке, вариант `flagged` (обоснование — `ds/screens/case-dssl.md` §Решения карты №2). Содержание другое: не «пересборка вместо отгруженного продукта», а **три утверждения сразу** — клиника реальная и под NDA, её имя, персонал, пациенты и цены не появляются нигде; все данные на кадрах синтетические; работа дошла до дизайна и прототипа, а не до боевой разработки.

Третье утверждение обязано стоять в шапке, а не в результате: без него строка исхода читается как «внедрено и работает».

### 3. Решений в `CaseDecisions` четыре

`CASE-09` разрешает 3–5. Пятое напрашивающееся — «счёт собирается из отметок врача, администратор не видит клинической записи» — по природе про роли и права, и живёт в `CaseReframe` и `CaseResult`, где у него есть контекст. В `CaseDecisions` оно конкурировало бы с четырьмя за внимание.

### 4. Ресёрч называется тем, чем он был

В блоке процесса — «conversations with a practising veterinarian», без слова `interviews`, без числа респондентов и без слова `research team`. Один человек и несколько разговоров — это разговоры, а не исследование выборки. То, чего разговоры не подтвердили, на странице названо гипотезой прямо: в проекте это дисциплина `🔬`, и здесь она работает на кейс, а не против него.

---

## Composition

Порядок блоков и их носители — те же, что у `CaseDSSL`. Ниже только содержание.

### CaseHeader — паттерн `CaseHeader`

| Часть | Содержание |
|---|---|
| Название | `Vet Clinic OS` |
| Строка сути | Система работы клиники, пересобранная вокруг тридцати секунд между пациентами — единственного времени, которое у врача реально есть |
| Мета | Client — `Private veterinary clinic — name withheld under NDA` · Product — `Clinic operations SaaS: schedule, medical record, invoicing` · Year — `2026` · Role — `Product Designer` · Platform — `Web — responsive: desktop, tablet in the room, owner’s phone` · Evidence — `Concept · domain input from one vet` · Prototype — `Live, on invented data`, ссылка |
| Строка исхода | Запись визита перестала быть объектом «всё или ничего»: след за тридцать секунд — вес, препарат, доза — создаётся в кабинете и сразу виден коллегам, полная запись дописывается позже. Цена: карта часами стоит заведомо неполной, и эта неполнота обязана быть видна каждому, кто её читает |
| Контекст команды | Team — `Sole designer — research, product decisions, design system and the prototype` · Research — `Conversations with a practising veterinarian; no research team` · Duration — `Two weeks, brief to working prototype` |
| Оговорка | `flagged`, по решению карты №2 |

- **Пара `Evidence` добавлена 2026-08-28** (находка `L2-1`, канонический разбор — `ds/screens/home.md` §FeaturedCase). Число «один» в строке стоит намеренно: это та же граница, которую держит решение карты №4 «ресёрч называется тем, чем он был». Строка дословно совпадает с обложкой на главной.
- ~190 слов

### CaseCover — `ScreenStack`, в колонке контейнера

Три кадра одного продукта, снятые одним прогоном: один viewport, один фон, одна обработка.

- **Решено на снятых кадрах 2026-08-25: передний — очередь дня** (`/app/vet-day-queue`), за ним карта пациента и расписание администратора. След визита в стопку не пошёл: он снят на планшетной ширине 768, а кадр планшета рядом с двумя десктопными перестаёт читаться как один продукт. След живёт в `CaseDecisions` 1, где ему и место — это доказательство решения, а не жанра.
- **Очередь дня выбрана передней не как самый узнаваемый экран, а как единственный, на котором проблема кейса видна до первой строки текста:** «3 visits are unfinished», и рядом — самый долго ждущий пациент. Карта пациента и расписание за ней показывают второе рабочее место и объём продукта.
- Подпись `ds-meta-sm`: что за экран и что **данные синтетические** (`CASE-22`).
- Без рамок устройств, перспективы и теней (`CASE-20`).

### CaseContext — «Between Thursday and Saturday, the visit did not exist.»

`SectionHead size=sm` + `ProseBlock`.

Содержание: день врача — 10–20 приёмов, программа открывается до и после приёма, но ни разу во время; блокнот в сумке, потому что писать приходится одной рукой стоя; вес вводится в систему и следом руками переносится в калькулятор в телефоне; состав услуг передаётся администратору голосом и не уточняется; карта дописывается дома в девять вечера по памяти. И следствие: в субботу принимает коллега, последняя запись в карте годичной давности, второе противовоспалительное поверх первого не назначено только потому, что врач взял трубку в выходной.

- Поле под артефакт пустое: «до» — это чужая система и бумажный блокнот, показывать нечего. `CASE-13` разрешает, `TECH-15` запрещает серую заглушку.
- ~230 слов

### CaseReframe — «The unit of work was never the visit. It was the trace.»

`SectionHead size=sm` + `ProseBlock` + `NoteBlock variant=emphasis`.

Содержание: бриф просил «закрывать визит быстрее», North Star был «доля визитов, закрытых в системе в день приёма». Разговоры с врачом опровергли собственное требование: между пациентами есть тридцать секунд, и выбор в них — сесть и внести или улыбнуться следующему; врач всегда улыбается следующему, двенадцать раз в день. Продукт, который требует закрыть визит целиком, откладывается целиком. Значит, объект двухуровневый: минимальный след, создаваемый за секунды и уже видимый коллегам, и полная запись, дописываемая позже. Метрика меняется следом — не полнота к концу смены, а наличие следа к моменту выхода из кабинета.

- **Отдельный блок, не абзац контекста** (`CASE-06`).
- Строка постановки (`NoteBlock emphasis`): сделать так, чтобы самая маленькая полезная запись переживала тридцать секунд, а всё остальное дописывалось потом.
- ~210 слов

### CaseProcess — «I checked the incumbent before I drew anything, and wrote down what I could not check.»

`SectionHead size=sm` + `ProseBlock` + ссылка на прототип + два `MediaFrame ratio=natural`.

Содержание: UX-аудит системы-лидера по эвристикам — **с явной границей покрытия**: экран приёма врача, расписание и кабинет владельца в скриншоты не попали, и выводов по ним не делалось. Ресёрч рынка и регуляторики, где три цифры помечены как непроверенные и в PRD без ручной сверки не пущены. Разговоры с практикующим врачом. Приоритизация: 31 Must из 62 требований, ядро приёма неделимо — одна фича из семи частей. ИА: sitemap на 44 экрана, три flow, двенадцать low-fi кадров. Дизайн-система. Двенадцать hi-fi кадров сквозной истории одного визита — от очереди врача до выписки в телефоне владельца, 31 edge case заведён скрытыми состояниями. React-прототип, каталог компонентов в Storybook, агентские прогоны по трём сценариям.

- **Ссылка на прототип** — `TextLink type=arrow external` с подписью над ней: живой прототип на синтетических данных.
- Артефакты: индекс тринадцати экранов сборки и матрица вариантов компонента из каталога.
- `CaseAI` — **[v2]**, как и в `CaseDSSL`.
- ~330 слов

### CaseFailure — «The screens passed my own audit. The prototype did not.»

`SectionHead size=sm` + `ProseBlock`.

Три настоящих провала:

1. **Экрана не существовало.** Свободный слот в расписании создавал визит без пациента, владельца и причины: сценарий записи был в sitemap, а экрана под него не было ни в макете, ни в спеке. Он появился только в прототипе и честно записан как `prototype-only` — то есть источник дизайна и сборка разошлись, и разошлись молча.
2. **Два индикатора сохранения на одном экране.** Продукт, у которого dealbreaker — потерянный черновик, показывал статус сохранения дважды: в шапке и в боковой панели. Два источника правды о сохранности — это ровно тот дефект, который нельзя допускать именно здесь.
3. **Компонент, который не был нужен.** `Card` заводился как контейнер: три варианта, ноль инстансов на всей странице. Инстансу в Figma нельзя добавить дочерний слой — компонент несёт ту анатомию, с которой создан, а реальным блокам нужно от двух до пяти элементов. Удалён на аудите, а не оставлен «на будущее».

- Самостоятельный блок, тот же кегль (`CASE-08`).
- ~220 слов

### CaseDecisions — «Four decisions, and what each one cost.»

Четыре `DecisionBlock`, структура `decision` → `why` → `cost`.

| # | Decision | Why | Cost | Артефакт |
|---|---|---|---|---|
| 1 | След и полная запись — одно состояние объекта в двух стадиях, а не два документа | Требование полноты за смену несовместимо с реальностью тридцати секунд; след обязан быть виден коллеге немедленно — иначе повторяется субботний приём по годичной карте | Карта часами стоит заведомо неполной, и неполнота обязана читаться всеми: коллегой, администратором, публикацией владельцу, которая след публиковать отказывается | след визита, планшет |
| 2 | Доза считается от веса, который уже в системе, и формула показывается целиком | Настоящая почти-ошибка — переставленная запятая при ручном переносе в сторонний калькулятор, а не незнание противопоказаний. Убрать перенос можно без справочника вообще | Видовых предупреждений о токсичности в MVP нет: риск, который волнует клинику и юриста, остаётся незакрытым и вынесен в отдельный go/no-go | расчёт дозы |
| 3 | Приватная зона врача получает собственный цвет в палитре, а не подпись | «Не нравится мне это» — класс данных, который сейчас не живёт нигде: блокнот выбрасывается, наблюдение теряется. Граница «владелец этого не увидит» обязана пережить ребрендинг, а подпись его не переживает | Один тон палитры занят навсегда и ни для чего другого не переиспользуется | карта пациента, приватная запись |
| 4 | Публикация владельцу — явное действие врача с предпросмотром от его лица | Преждевременная публикация черновика необратима: врач начнёт писать только то, что готов прочитать вслух, и тогда пусты и карта, и счёт, и выписка | Лишний шаг на каждом визите, и выписка не автоматизируется вовсе | предпросмотр выписки |

- ~540 слов на четыре

### CaseSystem — «Thirty-one components, and one I deleted.»

`SectionHead size=sm` + `ProseBlock` + сетка 3×2 (`CaseSystemGrid`).

Содержание: 85 Variables (54 примитива, 31 смысловой), 21 текстовый стиль, 31 логический компонент, 136 вариантов. Прибитых цветов ноль, текстовых узлов без стиля ноль, отвязанных инстансов ноль — проверено полным сканом, а не выборкой. Долг по geometry-биндингу не спрятан, а перечислен. Матрица покрывается скриптом, каталог — Storybook.

Шесть ячеек с состояниями в подписи (`CASE-10`, `US-18`): `SaveStatus` (saved · saving · unsaved · offline), `StatusTag`, `QueueRow`, `WeightReading`, `PrescriptionLine`, `EmptyState` (no results · nothing yet · could not load).

- ~200 слов

### CaseResult — «What got solved, and what it cost.»

`MetaList layout=statements` (четыре строки) + `NoteBlock variant=muted`.

| Метка | Утверждение |
|---|---|
| `Solved` | След визита существует до того, как врач выйдет из кабинета; счёт собирается из отметок врача, а не из вопроса администратора «что выставлять?»; владелец видит только опубликованное |
| `Sacrificed` | Видовые предупреждения о токсичности, лекарственный учёт и маркировка, приём денег и фискализация — вырезаны явно, с фиксацией, а не забыты |
| `Why the price was right` | Каждый отказ закрывает то, что продукт не может гарантировать сам: лицензируемый источник доз, чужую регуляторную машину, чужие деньги. Обещание любого из них ломается на первой же смене, а ломается вместе с ним доверие врача, которое не восстанавливается |
| `What changed in how I work` | Я перестал считать пройденным аудит, который прошли только экраны. Прототип нашёл экран, которого не существовало, и второй индикатор сохранения там, где потерянный черновик — dealbreaker |

**Строка NDA** (`muted`, `CASE-12`): клиника под NDA, бизнес-метрик нет и baseline тоже; метрики пилота определены как план измерения — и предложены самим врачом в наблюдаемых признаках: не куплен блокнот, уход в семь вместо девяти, администратор перестал спрашивать «что выставлять?». Ни одной метрики активности в слоте результата (`OUT-07`).

- ~240 слов

### CaseOutro — «This case is mostly a list of refusals.»

`SectionHead size=sm` + `ProseBlock` + `TextLink type=arrow` на следующий кейс — `Pawly`. Правка 2026-08-28, `L2-3`; разбор — `ds/screens/case-dssl.md` §`CaseOutro`.

**Прежняя строка карты была неверна дважды.** Она говорила «`mailto:`, как в `CaseDSSL`» — но в `CaseDSSL` стоял якорь, а не `mailto:`, — и обосновывала это тем, что «якорь `#contact` на сайте не существует», тогда как `ContactBlock` печатает `PageShell` на всех маршрутах и узел проверялся по собранной странице. Обе ошибки сняты вместе с самим CTA.

- ~30 слов

---

## Изображения

Снимаются с локальной сборки `d:\Claude-projects\Veterinary-clinic` скриптом `scripts/shoot-vet-frames.mjs`: viewport съёмки 1680×1000, сам десктопный экран продукта 1440 px, `deviceScaleFactor` 1.5, обложки 2000×1250 webp, естественные кадры — до 2000 px по длинной стороне. Обработка — только даунскейл, предметный кроп и кодирование. Playwright берётся из `d:\Claude-projects\b2b-dssl`, где он уже стоит: в `Veterinary-clinic` его нет, а продукт не тянет браузер ради съёмки.

Папка — `public/media/case-vet/`, ячейки системы — `public/media/case-vet/system-*.webp`, стопка обложки — `public/media/case-vet/cover/`.

| Блок | Кадр | Источник |
|---|---|---|
| `CaseCover` | стопка из трёх | `/app/vet-day-queue`, `/app/patient-card`, `/app/schedule` |
| `CaseContext` | — | пусто, обоснование выше |
| `CaseProcess` | индекс экранов сборки | `/` |
| `CaseProcess` | матрица вариантов компонента | Storybook, история `AllVariants` |
| `CaseDecisions` 1 | след визита за тридцать секунд | `/app/visit-quick-trace` |
| `CaseDecisions` 2 | расчёт дозы с формулой | `/app/dose-calculator` |
| `CaseDecisions` 3 | карта пациента с приватной записью | `/app/patient-card` |
| `CaseDecisions` 4 | предпросмотр выписки | `/app/discharge-preview` |
| `CaseSystem` | шесть фрагментов с состояниями: `SaveStatus`, `Input`, `ChoiceChip`, `WeightReading`, `TimeSlot`, `StatusTag` | Storybook, истории `AllVariants` |

**Все четыре артефакта решений пересняты после адаптивной переработки прототипа 31.08.2026.** Старые кадры были от 25 августа и больше не совпадали с реализованными страницами; старые координаты начинали `dose-calculator` внутри блока веса и резали заголовок, а `discharge-preview` показывал прежнюю композицию.

- `visit-quick-trace` теперь три соседних шага, а не одна узкая колонка. Кроп `2000×1140` снимает только пустую нижнюю часть артборда и идёт `artifactLayout=wide`; полный экран открывается через `zoomSrc`.
- `dose-calculator` оставляет правую панель целиком от заголовка до итога курса (`745×1395`, масштаб в боковой колонке **0,61**). Полная запись визита рядом остаётся в зуме.
- `patient-card-private` оставляет весь зарезервированный цветом блок без среза границы (`1595×300` en, `1595×310` ru) и идёт во всю ширину: в боковом слоте текст границы аудитории был физически нечитаем.
- `discharge-preview` сохраняет обе реализованные колонки — исключённый клинический контент слева и точный документ владельца справа (`1950×1240`) — и тоже идёт во всю ширину. Вырезана только служебная шапка клиники и внешний воздух.

Английская и русская пары проверены отдельно: одинаковая ширина не считается доказательством одинаковой вертикальной геометрии. Все четыре превью открывают исходный полный экран через `zoomSrc`.

**Состав ячеек определён каталогом, а не вкусом.** Широкие строки продукта — `QueueRow`, `PrescriptionLine`, `ScheduleRow` — в ячейки не пошли: матрица каталога `repeat(auto-fit, minmax(180px, 1fr))`, и строка на 600 px обрезается там посреди слова. Обрезанный вариант не может быть доказательством матрицы состояний. `EmptyState` не пошёл по другой причине: у него нет матрицы вовсе — standalone master, история `AllVariants` рисует один экземпляр, а подпись ячейки обязана перечислять состояния (`CASE-10`).

**Ширина ячейки задаётся при съёмке.** Истории идут `layout: centered`, корень ужимается по содержимому, и матрица схлопывается в одну колонку: ячейка выходила полосой 1:5. Скрипт ставит корню 760 px — четыре варианта в ряд, и все шесть ячеек приходят одной геометрии.

**Данные на всех кадрах выдуманы** — `src/data/clinicData.tsx` проекта: клички, владельцы, адреса `example.com`. Клиника в интерфейсе названа `Lesnaya Clinic` — это декорация прототипа, одна на все кадры; оговорка шапки называет её вымыслом прямо, иначе читатель принял бы её за заказчика.

**Три правки в проекте `Veterinary-clinic`, сделанные съёмкой.** Кадры показали то, чего не показывала сборка:

1. **Рельс навигации уехал вниз.** QA-волна H1 убрала `SaveStatus` из `SideNav`, а правило `.nav > :last-child { margin-top: auto }`, которым он прижимался к низу, осталось и подхватило `<nav>`: на экране высотой 1147 рельс читался пустым. Правило снято. Этот дефект стал третьим абзацем `CaseFailure` — он про то же, про что и весь блок.
2. **Кириллица в каталоге.** Истории `StatusTag`, `ChoiceChip` и `Button` держали русские подписи — `Статус визита`, `4,8 кг`, `Сохранить приём`. На английской странице кейса это читалось бы как недоделка. Переведены, `storybook-static` пересобран (он в `.gitignore`, в репозиторий не идёт).
3. Пословная сверка всех источников кадров — семи маршрутов прототипа и семи историй каталога — прогоном по `innerText`: кириллицы 0 (`US-19`).

Все кадры — `MediaFrame ratio=natural` с `zoom`, один `MediaZoom` на маршрут: в колонке страницы кадр идёт третью исходника, и таблица веса или очередь дня там не читаются.

---

## Бюджет слов (`CASE-15`, лимит ~2500)

| Блок | Слов |
|---|---|
| CaseHeader | 190 |
| CaseContext | 230 |
| CaseReframe | 210 |
| CaseProcess | 330 |
| CaseFailure | 220 |
| CaseDecisions | 540 |
| CaseSystem | 200 |
| CaseResult | 240 |
| CaseOutro | 30 |
| **Итого** | **2190** |

**По факту сборки — 2499 слов** на всю страницу, считая навигацию и подвал (`dist/work/vet-clinic/index.html`, подсчёт 2026-08-25). Лимит `CASE-15` пройден, но запас — один десяток слов, а не три сотни: блоки вышли плотнее бюджета почти везде, и текст ужимался пятью проходами. **Следующая правка текста обязана начинаться с пересчёта**, а не заканчиваться им. Для сравнения тем же счётчиком: `CaseDSSL` — 2164.

**Пересчёт 2026-08-28, после правок `L2-1`, `L2-3` и `L1-3`.** Страница вернулась туда же, откуда уходила: ~2 500. Добавились пары `Evidence` и `Prototype` в мете (+12) и граница проверки в `CaseResult` (+19); ушёл контактный CTA в `CaseOutro` (−3). Плата взята ужатием двух мест, а не удалением содержания: середина строки NDA (−13) и первый абзац `CaseSystem`, где «31 logical components» дублировало собственный заголовок блока (−8).

**Две разные бухгалтерии, и их нельзя сравнивать между собой.** Здесь счёт идёт **по собранной странице** и включает навигацию с подвалом. В `ds/screens/case-agent-ops.md` таблица считает **сумму блоков текста** без хрома — оттого её итог 2 477 и здешние 2 499 не про одно и то же. Требование `CASE-15` говорит про объём кейса, то есть ближе ко второй мере; счёт по странице здесь строже намеренно и остаётся строже.

**Пересчёт 31.08.2026, строка `Platform`.** `Web — desktop, tablet in the room, owner’s phone` → `Web — responsive: desktop, tablet in the room, owner’s phone`. Прототип доведён до полного адаптива 360–1440, и перечень устройств этого не говорил: он называл, где продуктом пользуются, а не то, что сборка держит диапазон. Форма выбрана самая дешёвая из трёх — квалификатор впереди перечня, **+1 слово** (третий счётчик: 2 472 → 2 473). Запас здесь меньше всех: по здешней бухгалтерии страница шла 2 499 при лимите 2 500. **Следующая правка текста этого кейса обязана начинаться с высвобождения слов, а не с добавления.** Разбор — `audit/visual-density-2026-08-31.md`.

**Кадр диапазона, 31.08.2026, и первый перерасход по `CASE-15`.** В конце `CaseReframe` встал `range` — очередь дня на 1440, 768 и 390 px. Колонки три, а не две, как у Agent Ops и DSSL: планшет назван платформой прямо в мете этого кейса, и диапазон обязан его показать. Кадр разбивает самый длинный прогон страницы: **786 → 475**, остаток 325.

Цена: подпись **+14 слов**, самая короткая из трёх кейсов — 2 473 → 2 487 третьим счётчиком. По здешней, самой строгой бухгалтерии это **≈ 2 514 при лимите 2 500**, то есть первый осознанный перерасход. Он не закрыт встречным сокращением намеренно: высвобождение 14 слов означало бы правку уже выверенной прозы владельца — редакторское решение, а не техническое, и оно за владельцем (`L3`). **Открытый пункт: либо снять 14 слов в `CaseSystem` или `CaseResult`, либо признать лимит «~2500» выдержанным.** Разбор — `audit/visual-density-2026-08-31.md` §9.

**Калибровка счётчика.** Внешний счётчик может давать другое число: тот, которым считали 2026-08-28, идёт примерно на полсотни слов ниже здешнего — он не берёт содержимое атрибутов и одиночную пунктуацию. Разница снята по `CaseDSSL`: 2 164 против 2 111 на том же состоянии страницы. Кто считает другим инструментом — сначала калибруется по DSSL, потом делает вывод про лимит.

---

## Что этот экран меняет на главной

Решение владельца 2026-08-25: **две обложки, DSSL первой**.

- `src/copy/home.ts` §`featured` становится списком из двух записей; вёрстка `src/pages/index.astro` повторяет паттерн `FeaturedCase` вторым экземпляром, новых компонентов не заводится.
- Список `works` теряет запись `Vet Clinic OS`: заголовок «Three more projects» → «Two more projects», остаются Pawly и RUUN.
- `hero.caseEntry` — строка «Read the case: B2B Partner Portal» — пересматривается: с двумя кейсами она называет один. Решение — при сборке.
- ~~`CaseOutro` кейса DSSL: замена на `Next case`~~ — **закрыто 2026-08-28**, находка `L2-3`. Сделано сразу на всех четырёх кейсах и обеих локалях, а не только у DSSL.

## Что этот экран добавит в ДС

Ничего. Компоненты, паттерны и токены те же, что у `CaseDSSL`; в этом и смысл шаблона. Если при сборке понадобится новый носитель — это сигнал, что кейс ломает шаблон, и решается до вёрстки, а не правкой на ходу.

---

## Осталось открытым

### 1. Репозиторий `Veterinary-clinic` противоречит этой странице

`CLAUDE.md`, `outputs/brief.md` и `outputs/interview_primary_persona.md` там заявляют «живого заказчика нет» и «интервью — симуляция». Страница говорит «реальная клиника под NDA» и «разговоры с практикующим врачом». Репозиторий приватный, но противоречие живёт, и правится оно там, а не здесь.

### 2. Заглушки, унаследованные от главной

Те же, что у `CaseDSSL`. Кейс их не добавляет и не чинит.

### 3. Правка `SideNav` живёт только локально

Дефект рельса починен в рабочей копии `d:\Claude-projects\Veterinary-clinic`, но не закоммичен и не выложен. Прототип на Vercel, на который кейс ссылается, до пуша показывает рельс со старым дефектом — то есть страница и продукт за ссылкой расходятся. Решение по коммиту и пушу — за владельцем.

### 4. `CaseOutro` кейса DSSL всё ещё тупиковый

`ds/screens/case-dssl.md` предусматривал замену выхода на `Next case → Vet Clinic OS` в v1.1. Кейс существует, замена не сделана: в объём этой задачи она не входила.

---

## Приёмка

Прогон 2026-08-25, после сборки.

| Проверка | Как | Результат |
|---|---|---|
| `astro check` | `npm run check` | 0 ошибок, 0 предупреждений |
| Сборка | `npm run build` | проходит, 8 страниц, `/og/work-vet-clinic.png` генерируется |
| Мёртвые скоупленные правила | `npm run check:css` | 0 на всех семи маршрутах. Маршрут `/work/vet-clinic` добавлен в список скрипта тем же проходом — без строки он не проверялся бы вовсе |
| Объём ≤ 2500 слов (`CASE-15`) | подсчёт по `dist/work/vet-clinic/index.html` | **2499 слов**, проходит впритык — см. бюджет выше |
| `NoScriptView` ≥ 1500 знаков (`TECH-03`) | подсчёт текста до JS | **14 089 знаков**, проходит |
| Обоснование у каждого решения (`CASE-09`) | четыре из четырёх | проходит — `DecisionBlock` не даёт собрать решение без `why` и `cost` |
| Строка NDA присутствует (`CASE-12`) | разметка `CaseResult` | проходит |
| Ни одной выдуманной метрики (`US-09`, `OUT-07`) | пословный просмотр `CaseResult` | проходит. Бизнес-цифр нет; числа кейса — состав системы и объём работы, метрики пилота названы планом измерения |
| Ни одного додуманного факта | сверка шапки с закрытой фактурой | проходит: Team, Research, Duration, судьба продукта — только подтверждённое владельцем 2026-08-25 |
| Обезличивание (`US-19`) | прогон по `innerText` семи маршрутов прототипа и семи историй каталога | проходит: кириллицы 0, реальных названий и реквизитов нет. Клиника на кадрах вымышленная, оговорка шапки называет это прямо |
| Вес изображений (`TECH-10`) | WebP, ширина ≤ 2000 | 15 файлов, 0,97 МБ на весь кейс |
| Мобильная раскладка на 360 px | Chromium 360×740, полная прокрутка | проходит: горизонтальной прокрутки нет при высоте документа 19 885 px, 0 ошибок в консоли |
| Десктоп 1440 | полная прокрутка | проходит: 0 ошибок, 0 битых запросов, горизонтальной прокрутки нет |
| Цель нажатия на мобильном ≥ `space-12` | замер каждой ссылки и кнопки | проходит после правки: ссылка на прототип была 26 px, стала 48. **Правка в шаблоне, то есть сразу на обоих кейсах** — на DSSL эта ссылка появилась после его приёмки и замер не проходила |
| `ReducedMotionState` | эмуляция `prefers-reduced-motion` | проходит: скрытых узлов в `main` 0 |
| `DirectEntryState` | прямой вход на `scrollY 2500` | паритет с `CaseDSSL`: те же 8 узлов скрыты на обеих страницах — это состояние закрытого `MediaZoom`, а не пропавшее содержание |
| Две обложки на главной | `#work` в собранном HTML | проходит: два узла `.featured__link`, промежуток 128 px (`section-gap` на `bp-lg`) |
