# Foundation — Site-portfolio

**Бриф:** сдержанное editorial-портфолио продуктового дизайнера, web desktop+tablet+mobile, en с каркасом под ru, характер через типографику и движение, не через декор.
**Статус:** принят 2026-08-24. Исполняемый носитель — `ds/tokens.css` и `ds/motion.js`; этот файл держит обоснования решений. Figma в проекте не используется.

---

## Слой 1 — примитивные токены

### Палитра

Нейтралы уведены в тёплую сторону: чистый серый на крупных плоскостях читается технически, слегка тёплый — как бумага. Ink не чёрный (`#1B1B18`), чтобы крупная типографика не жгла на белом.

| Токен | Hex | Назначение |
|---|---|---|
| `gray-00` | `#FFFFFF` | основной фон |
| `gray-50` | `#F7F7F5` | фон-бумага, подложка секций |
| `gray-100` | `#EFEFEC` | плейсхолдер медиа до загрузки |
| `gray-200` | `#E1E1DC` | волосяная линия, разделители перечня |
| `gray-300` | `#C7C7C0` | декоративная линия на тёмном, disabled-графика |
| `gray-500` | `#6E6E67` | метаданные, вторичный текст — **проходит AA 5.14:1 на белом** |
| `gray-700` | `#53534C` | текст пониженного веса, 7.75:1 |
| `gray-900` | `#1B1B18` | ink, основной текст, 17.26:1 |
| `gray-950` | `#0D0D0B` | инверсная поверхность |

Акцент — приглушённый рыжий. Выбран против дефолтного синего: в почти монохромной композиции он читается как решение, а не как «ссылка по умолчанию», и при этом не датируется через два года.

| Токен | Hex | Контраст |
|---|---|---|
| `accent-300` | `#E8845C` | 7.31:1 на `gray-950` — ссылка на инверсной поверхности |
| `accent-500` | `#C1440E` | 5.12:1 на белом, 4.77:1 на `gray-50` — ссылка, кольцо фокуса |
| `accent-700` | `#94330A` | 7.67:1 на белом — hover |
| `accent-900` | `#5E2006` | резерв под pressed |

| Токен | Hex | Назначение |
|---|---|---|
| `success-500` | `#2F7D4F` | 5.04:1 на белом — подтверждение копирования email (`SCR-07`) |

`warning`, `error`, `info` не заводятся: в продукте нет форм (`OUT-04`), валидации и статусов. Появятся, когда появится носитель.

### Типографика

- `font-sans` = **Manrope** — display и body
- `font-mono` = **JetBrains Mono** — метаданные, роль, год, платформа, локальное время

Шкала на 11 ступеней. Верхние три — display, только desktop, в коде через `clamp()`.

| Токен | px | Применение |
|---|---|---|
| `size-xs` | 12 | mono-метаданные, label |
| `size-sm` | 14 | подписи, футер |
| `size-base` | 16 | body |
| `size-lg` | 18 | body кейса |
| `size-xl` | 20 | lead-абзац |
| `size-2xl` | 24 | заголовок блока |
| `size-3xl` | 32 | заголовок секции |
| `size-4xl` | 48 | заголовок кейса |
| `size-5xl` | 64 | display малый |
| `size-6xl` | 96 | display основной |
| `size-7xl` | 128 | имя в hero, desktop |

| Токен | Значение |
|---|---|
| `weight-regular` | 400 |
| `weight-medium` | 500 |
| `weight-semibold` | 600 |
| `weight-bold` | 700 |
| `weight-extrabold` | 800 |

Межстрочный. База 1.6 вместо 1.5 — +5% под кириллицу второй волны, заложено сразу, чтобы RU-контент не ломал ритм.

| Токен | Значение | Применение |
|---|---|---|
| `lh-display` | 1.02 | 5xl–7xl |
| `lh-tight` | 1.15 | 3xl–4xl |
| `lh-snug` | 1.30 | xl–2xl |
| `lh-base` | 1.60 | body |
| `lh-relaxed` | 1.75 | длинный текст кейса |

Межбуквенный. На крупных кеглях отрицательный, на mono-метаданных положительный.

| Токен | Значение | Применение |
|---|---|---|
| `tracking-display` | −1.5% | 6xl–7xl |
| `tracking-tight` | −1.0% | 4xl–5xl |
| `tracking-snug` | −0.5% | 2xl–3xl |
| `tracking-none` | 0% | body |
| `tracking-wide` | +5% | mono-label в верхнем регистре |

### Радиусы

`radius-none` = 0 · `radius-sm` = 4 · `radius-md` = 8 · `radius-lg` = 16

Проект по умолчанию работает на `radius-none`. Остальные — резерв, чтобы расширение не потребовало правки примитивов.

### Отступы

`space-1` = 4 · `space-2` = 8 · `space-3` = 12 · `space-4` = 16 · `space-6` = 24 · `space-8` = 32 · `space-12` = 48 · `space-16` = 64 · `space-24` = 96 · `space-32` = 128 · `space-48` = 192

Имя = значение ÷ 4. Верхние три ступени — межсекционные: без них «много воздуха» из брифа не собрать.

### Тени

`shadow-sm` = 0 1 2 rgba(27,27,24,.06) · `shadow-md` = 0 4 12 rgba(27,27,24,.08) · `shadow-lg` = 0 12 32 rgba(27,27,24,.10)

Мягкие, на ink, без бренд-цвета. В продукте не применяются нигде, кроме возможной панели `MobileNav`. Заведены как резерв.

### Сетка и брейкпоинты

| Токен | Значение |
|---|---|
| `bp-sm` | 360 |
| `bp-md` | 768 |
| `bp-lg` | 1024 |
| `bp-xl` | 1440 |
| `container-max` | 1440 |
| `margin-sm` | 24 |
| `margin-md` | 48 |
| `margin-lg` | 80 |
| `columns-sm` | 4 |
| `columns-md` | 6 |
| `columns-lg` | 12 |
| `gutter` | 24 |

**Меры колонки — добавлено при расширении каталога 2026-08-24.** Потолок длины строки: до расширения его не было, потому что не было ни одного текстового блока. `ProseBlock` и `NoteBlock` дали десять носителей сразу.

| Токен | Значение |
|---|---|
| `measure-720` | 720 |
| `measure-800` | 800 |

360 px — расчётная ширина, не крайний случай: мобильная главная спроектирована самостоятельно (`SCR-09`, `TECH-08`).

### Motion

Слоя нет в зашитом минимуме директивы. Добавлен, потому что в этом продукте движение — носитель характера, а не отделка, и без токенов оно разъедется по компонентам.

Длительности, мс:

| Токен | Значение | Применение |
|---|---|---|
| `duration-instant` | 120 | смена состояния без перемещения |
| `duration-fast` | 240 | hover |
| `duration-base` | 400 | локальные переходы |
| `duration-page` | 600 | переход между страницами |
| `duration-slow` | 700 | раскрытие изображения |
| `duration-reveal` | 900 | reveal типографики |

Кривые, строками — имена GSAP, чтобы токен переносился в код без перевода:

| Токен | Значение |
|---|---|
| `ease-standard` | `power2.out` |
| `ease-entrance` | `power3.out` |
| `ease-exit` | `power2.in` |
| `ease-expressive` | `expo.out` |

Задержки каскада, мс: `stagger-tight` = 40 · `stagger-base` = 80 · `stagger-loose` = 120

---

## Слой 2 — смысловые токены

Прибитых значений здесь нет ни одного: каждый токен — ссылка на примитив. Это инвариант, на нём держится ребрендинг.

### Поверхности

- `surface-default` → `gray-00`
- `surface-subtle` → `gray-50`
- `surface-inverse` → `gray-950`
- `surface-media` → `gray-100`
- `surface-action-primary` → `gray-900`
- `surface-action-primary-hover` → `gray-700`

Кнопка — ink, не акцент. Акцент отдан ссылкам и фокусу; если он же зальёт кнопку, монохром перестанет читаться как решение.

### Текст

- `text-default` → `gray-900`
- `text-subtle` → `gray-700`
- `text-muted` → `gray-500`
- `text-meta` → `gray-500`
- `text-on-inverse` → `gray-00`
- `text-on-action` → `gray-00`
- `text-link` → `accent-500`
- `text-link-hover` → `accent-700`
- `text-link-on-inverse` → `accent-300`
- `text-success` → `success-500`

### Обводки

- `border-default` → `gray-200`
- `border-strong` → `gray-500`
- `border-inverse` → `gray-700`
- `border-focus` → `accent-500`

`border-strong` намеренно уведён в `gray-500`, а не в `gray-300`: граница, несущая смысл, обязана давать 3:1 по WCAG 1.4.11. `gray-300` даёт 1.70 и годится только на декоративную линию.

### Фокус

- `focus-ring-color` → `accent-500`
- `focus-ring-width` → `focus-width` (примитив, 2)

### Меры колонки

- `measure-text` → `measure-720` — тело кейса и About, основная колонка чтения
- `measure-lead` → `measure-800` — ведущий абзац, строка сути, строки компромисса в `CaseResult`

Потолок ставится на колонку, а не на секцию: секция держит контейнер и поля, мера отвечает только за длину строки.

### Типографические роли

- `font-body` → `font-sans`
- `font-display` → `font-sans`
- `font-meta` → `font-mono`

### Motion

| Смысл | Состав | Где живёт |
|---|---|---|
| `motion-hover` | `duration-fast` + `ease-standard` | CSS и JS |
| `motion-state` | `duration-instant` + `ease-standard` | CSS и JS |
| `motion-reveal-text` | `duration-reveal` + `ease-entrance` + `stagger-base` | только JS |
| `motion-reveal-media` | `duration-slow` + `ease-expressive` | только JS |
| `motion-page-out` | `duration-page` + `ease-exit` | только JS |
| `motion-page-in` | `duration-page` + `ease-entrance` | только JS |

Разделение не декоративное: CSS-переход умеет отработать hover и смену состояния, но не умеет каскад и не умеет переход между страницами. Первые два продублированы в обоих носителях, остальные четыре существуют только в `ds/motion.js`.

Все шесть обязаны отключаться по `prefers-reduced-motion`, включая переходы между страницами (`MOT-01`). Точек отключения тоже две: `gsap.matchMedia()` через `withMotionPreference()` для JS-твинов и медиазапрос в `tokens.css` для CSS-переходов, которых GSAP не видит.

---

## TextStyles

Двенадцать стилей с префиксом `DS/`. `fontSize` каждого привязан к примитиву, межстрочный и межбуквенный заданы в процентах. Цвет в стиле не хранится — он приходит отдельно, через `fills` с привязкой к смысловому токену.

| Стиль | Семейство | Размер | lh | tracking |
|---|---|---|---|---|
| `DS/Display/7xl` | Manrope ExtraBold | `size-7xl` | 102% | −1.5% |
| `DS/Display/6xl` | Manrope ExtraBold | `size-6xl` | 102% | −1.5% |
| `DS/Display/5xl` | Manrope Bold | `size-5xl` | 102% | −1.0% |
| `DS/Heading/4xl` | Manrope Bold | `size-4xl` | 115% | −1.0% |
| `DS/Heading/3xl` | Manrope SemiBold | `size-3xl` | 115% | −0.5% |
| `DS/Heading/2xl` | Manrope SemiBold | `size-2xl` | 130% | −0.5% |
| `DS/Heading/xl` | Manrope Medium | `size-xl` | 130% | 0% |
| `DS/Body/lg` | Manrope Regular | `size-lg` | 175% | 0% |
| `DS/Body/base` | Manrope Regular | `size-base` | 160% | 0% |
| `DS/Body/sm` | Manrope Regular | `size-sm` | 160% | 0% |
| `DS/Meta/sm` | JetBrains Mono Regular | `size-sm` | 160% | 0% |
| `DS/Meta/xs` | JetBrains Mono Medium | `size-xs` | 160% | +5% |

### Ступени по брейкпоинтам

Крупные роли уменьшаются медиазапросом в `ds/tokens.css`, а не пропами компонентов. Ступень — соседняя строка шкалы, не произвольное число.

| Роль | ≥1024 | 768–1023 | <768 |
|---|---|---|---|
| `DS/Display/7xl` | 128 | 64 | 48 |
| `DS/Display/6xl` | 96 | 48 | 32 |
| `DS/Display/5xl` | 64 | 48 | 32 |
| `DS/Heading/4xl` | 48 | 48 | 32 |
| `DS/Heading/3xl` | 32 | 32 | 24 |
| `DS/Heading/2xl` | 24 | 24 | 20 |
| `DS/Heading/xl` | 20 | 20 | 20 |

Display-ступени стояли с постановки ДС. Heading-ступени добавлены **2026-08-24**, при сборке Home: три мобильных узла требовали мельче, и единственная альтернатива — прибитый `font-size` в компонентах — запрещена контрактом.

`DS/Heading/xl` не съезжает: 20 px — нижняя граница, при которой заголовок ещё отличается от тела. Body- и meta-роли не съезжают вовсе: 16 px на мобильном не уменьшают, его увеличивают.

**Одно расхождение с вайрфреймом принято сознательно.** `ia/wireframes/mobile-home.md` просит адрес email 20 px, ступень даёт 24. Роль важнее числа из макета — тот же принцип, по которому `ProseBlock` берёт 18 вместо 20. Проверяется на живой странице, когда будет известен реальный адрес: если он не влезает в 327 px при 24 — это повод пересмотреть ступень, а не завести исключение в компоненте.

---

## Слой 3 — компонентные токены

На стартовом наборе не нужен. Добавляется отдельной директивой при расширении компонентов вариантами и состояниями.

---

## Базовые компоненты

Семь, по реальному инвентарю `ia/screens-inventory.md`. Каждый — настоящий `ComponentNode` / `ComponentSet`, пригодный для `createInstance()`.

| Компонент | Варианты | Откуда в инвентаре |
|---|---|---|
| `Navbar` | `Breakpoint=desktop` · `Breakpoint=mobile` | `Nav` (IA-05), `MobileNav` (№33) |
| `TextLink` | `Type=inline` · `Type=arrow` · `Type=nav` | ссылки всех маршрутов, `CaseOutro`, `AboutShort` |
| `CopyEmail` | `State=default` · `State=copied` | `ContactBlock` (`SCR-07`), единственный контрол продукта |
| `MediaFrame` | `Ratio=wide` · `Ratio=portrait` | `FeaturedCase`, медиа кейса, `DevelopmentStrip` |
| `WorkRow` | `Layout=desktop` · `Layout=mobile` | `WorksList` (`WRK-02…05`), некликабельная строка |
| `MetaList` | `Layout=inline` · `Layout=stacked` | `CaseHeader` (`CASE-01…04`), подпись роли `DEV-02` |
| `SectionHead` | `Size=lg` · `Size=sm` | заголовки секций главной и блоков кейса |

Отдельного `Button` нет: единственная кнопка продукта — `CopyEmail`, у неё своя логика состояния и `aria-live`. Заводить универсальный `Button` ради одного применения — тот же мёртвый компонент, от которого мы ушли.

`SkipLink` и видимый фокус (`TECH-12`, №35) — не компонент, а глобальный стиль на `border-focus` + `focus-ring-width`. Живёт в контракте, не в UI-ките.
