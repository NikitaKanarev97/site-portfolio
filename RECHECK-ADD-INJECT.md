# Сообщения для вставки в три запущенных чата перепроверки

Вставляются в **уже открытые** чаты A, B, V. Новые чаты не открываются.
Вставлять сейчас, не дожидаясь конца фазы 1: дополнение меняет матрицу ширин,
и домеренный прогон на шести ширинах всё равно придётся повторить.

Копировать текст между разделителями, без заголовков этого файла.

---

## Чат A — Agent Ops Console (`D:\Claude-projects\Agent-ops-console`)

```
Останови текущий прогон и не продолжай его в прежнем виде. Появился новый вход:
владелец лично прошёл собранный прототип, снял экраны и свёл наблюдения в
постановку. Это меняет объём и метод фазы 1.

Прочитай целиком, в этом порядке:

1. D:\Claude-projects\Site-portfolio\RECHECK-ADD-COMMON.md
2. D:\Claude-projects\Site-portfolio\RECHECK-A-ADD.md
3. D:\Claude-projects\Site-portfolio\PROMPT-AGENT-OPS-visual-responsive-fixes.md

Что меняется.

Первое. Пункты владельца подтверждены наблюдением в живой сборке. Доказывать их
существование не нужно. По каждому нужны корневая причина в коде — файл, строка,
компонент или токен — и весь класс вокруг неё: другие места, где та же причина
даёт тот же результат. Список владельца это карта типовых промахов исполнителя,
а не полный перечень дефектов.

Второе. Матрица автоматических проб становится 360, 390, 480, 768, 800, 900,
1024, 1200, 1280, 1440. Владелец видит поломку таблицы Cluster Review и
раскладки шапки на 800 — ровно между твоими 768 и 1024. Прежняя шестёрка ширин
пропускала промежутки по построению.

Третье. В харнесс добавляются пробы P1-P18 из общего дополнения, результаты
пишутся в measurements.json ключом ownerProbes. Прогон проб — по всем маршрутам,
а не только по тем, где владелец заметил дефект. Класс закрывается целиком.

Четвёртое. Отдельно перепроверь закрытие AQ-002 на 800 и 900: схлопывание
COMMITMENT объявлено исправленным и отмечено PASS на 768 и 1024, а владелец
видит поломку в промежутке. И проверь численно --field-visual-height против
высоты контрола ДС, против --bar-top и против порога 44 px на sm, md, lg —
жалоба на слишком низкое поле поиска садится ровно на переделку Input.

Фаза не меняется: read-only, ничего не чинишь, фаза 2 только по моей команде.
Список владельца целиком входит в утверждённый объём фазы 2, апрув по пунктам
не нужен.

Перед тем как продолжить, ответь коротко: что из уже сделанного придётся
перегнать из-за новых ширин и проб, и сколько на это уйдёт. Дальше работай.
```

---

## Чат B — B2B Partner Portal (`D:\Claude-projects\b2b-dssl`)

```
Останови текущий прогон и не продолжай его в прежнем виде. Появился новый вход:
владелец лично прошёл собранный прототип, снял экраны и свёл наблюдения в
постановку. Это меняет объём и метод фазы 1.

Прочитай целиком, в этом порядке:

1. D:\Claude-projects\Site-portfolio\RECHECK-ADD-COMMON.md
2. D:\Claude-projects\Site-portfolio\RECHECK-B-ADD.md
3. D:\Claude-projects\Site-portfolio\PROMPT-B2B-visual-responsive-fixes.md

Что меняется.

Первое. Твоя главная зацепка больше не гипотеза. Строка «No findings» в
artifacts/adaptive-qa/findings.md опровергнута фактически: владелец нашёл восемь
групп дефектов глазами, без харнесса, на основных маршрутах — синие системные
рамки при клике мышью, шапка ниже контента, развал каталога и Orders около
1200 px, фильтры за границей контейнера, узкие колонки в XLS-разборе. Работай
как по проекту, где QA не проводился вообще.

Второе. Пункты владельца подтверждены наблюдением. Доказывать их существование
не нужно. По каждому нужны корневая причина в коде — файл, строка, компонент или
утилита — и весь класс вокруг неё. Список владельца это карта типовых промахов
исполнителя, а не полный перечень дефектов.

Третье. Матрица автоматических проб становится 360, 390, 480, 768, 800, 900,
1024, 1200, 1280, 1440. Владелец прямо называет 1200 для каталога и заказов и
диапазон 390-1200 для шапки. Между твоими 1024 и 1280 не было ни одной точки.

Четвёртое. В харнесс добавляются пробы P1-P18 из общего дополнения, результаты
пишутся в measurements.json ключом ownerProbes. Прогон — по всем маршрутам, а не
только по названным владельцем. Класс закрывается целиком.

Пятое, проектное. Проверь гипотезу о причине развала на 1200: в диффе 361
префикс md: против 49 lg:, 21 xl: и 3 2xl: — раскладка переключается один раз на
768 и живёт без промежуточного шага до самого десктопа. Найди все места, где
md:-раскладка не уточнена на lg:/xl:, и особенно пары «боковая панель плюс
таблица». Отдельно по фокусу: где стоит focus: вместо focus-visible:, где
outline-none не заменён кольцом ДС, где ring рисуется поверх собственного border
компонента. Отдельно по фильтрам: input[type=checkbox] как flex-элемент без
shrink-0 — прямой кандидат в причину «высокого узкого прямоугольника».

Шестое. Все восемь пунктов прогоняются в обеих локалях. Владелец смотрел одну;
русские строки длиннее на 20-30 % и часть пунктов на RU проявится раньше по
ширине.

Фаза не меняется: read-only, ничего не чинишь, фаза 2 только по моей команде.
Список владельца целиком входит в утверждённый объём фазы 2, апрув по пунктам
не нужен.

Перед тем как продолжить, ответь коротко: что из уже сделанного придётся
перегнать из-за новых ширин и проб, и сколько на это уйдёт. Дальше работай.
```

---

## Чат V — Vet Clinic OS (`D:\Claude-projects\Veterinary-clinic`)

```
Останови текущий прогон и не продолжай его в прежнем виде. Появился новый вход:
владелец лично прошёл собранный прототип, снял экраны и свёл наблюдения в
постановку с дефектами A-J. Это меняет объём и метод фазы 1.

Прочитай целиком, в этом порядке:

1. D:\Claude-projects\Site-portfolio\RECHECK-ADD-COMMON.md
2. D:\Claude-projects\Site-portfolio\RECHECK-V-ADD.md
3. D:\Claude-projects\Site-portfolio\PROMPT-VET-CLINIC-visual-responsive-fixes.md

Третий файл написан как задача на реализацию. В твоём чате он работает иначе: в
фазе 1 ты его не выполняешь, а локализуешь причины и достраиваешь до класса.
Требования к проверке из его раздела 5 и критерии из раздела 6 переходят в фазу 2.

Что меняется.

Первое. Дефекты A-J подтверждены наблюдением в живой сборке. Доказывать их
существование не нужно. По каждому нужны корневая причина в коде — файл, строка,
компонент или токен — и весь класс вокруг неё. Список владельца это карта
типовых промахов исполнителя, а не полный перечень дефектов.

Второе. Матрица автоматических проб становится 360, 390, 480, 768, 800, 900,
1024, 1200, 1280, 1440. Владелец многократно называет 800 — между твоими 768 и
1024; дефекты B, C1, E2 и F3 привязаны именно к этой ширине.

Третье. В харнесс добавляются пробы P1-P18 из общего дополнения, результаты
пишутся в measurements.json ключом ownerProbes. Прогон — по всем маршрутам, а не
только по названным владельцем. Класс закрывается целиком.

Четвёртое, проектное. Дефекты C1, E2 и F3 — не три находки, а одна причина в
трёх местах: двухколоночная раскладка включается по достижении порога, без
проверки, что колонки помещаются. Дефекты C2, H и J проверь против расхождения
ритма между V3Screens.module.css, V4Screens.module.css, V5Screens.module.css и
общим Screens.module.css — приложи таблицу «маршрут → набор уникальных левых
краёв» по пробе P10. Дефекты D2, D3 и E1 — один компонент Input, и E1 смыкается
с твоим блоком состояний: если серая заливка означает readOnly, её появление на
редактируемом поле это ложное состояние того же рода, что ложная красная
подсветка. Дефект J имеет повышенную severity: owner-ветка, телефон основной
носитель, обрезанный левый край на 360.

Пятое. Пробы требуют браузера, а Playwright в этом проекте не установлен.
Правило прежнее: скажи мне и дождись подтверждения, прежде чем ставить chromium.
Это единственное изменение среды, которое тебе разрешено предложить.

Фаза не меняется: read-only, ничего не чинишь, фаза 2 только по моей команде.
Дефекты A-J целиком входят в утверждённый объём фазы 2, апрув по пунктам не
нужен.

Перед тем как продолжить, ответь коротко: что из уже сделанного придётся
перегнать из-за новых ширин и проб, и сколько на это уйдёт. Дальше работай.
```

---

## Чего ждать в ответ

Каждый чат должен сначала сказать, что перегоняет и почему, и только потом
продолжить. Если чат сразу побежал мерить, не ответив, — это признак, что
дополнение он не прочитал; переспроси прямым вопросом.
